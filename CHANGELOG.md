# 迭代记录

## v2.12.0

一次面向**语言覆盖与降级场景可读性**的功能拓展（配置版本 `CONFIG_VERSION` 保持 `1.5`，配置文件不新增字段）。
语言选择仍只写入 `localStorage`（键 `gridsneaker:locale`），**不进入配置、导出与分享链接**；
语言切换器旁新增的标识与兜底提示块的改动都只影响顶栏与降级提示，对既有用户无行为变化。

### 新增

- **语言切换器双语标识**：顶栏语言选择器左侧固定显示「Language 语言」——
  实现为 `<label class="lang-tag" for="lang-select">`（`Language` 用强调色、`语言` 用次要色），
  点击即聚焦下拉框，**不覆盖控件区域、不拦截其交互**；靠右对齐（`margin-left: auto`）与选择器成组，
  窄屏折行时也不挤占其它元素。
- **新增 6 种界面语言**：한국어（`ko`）· Français（`fr`）· Deutsch（`de`）· Español（`es`）·
  Português（`pt`）· Русский（`ru`），界面语言总数达 **10 种**；
  各语言包按 `zh-CN` 规范键集逐条对齐（**1670 条 / 包**），术语经统一对照（如
  ko `에이전트 / 뱀 머리 / 세포 자동자`、fr `agent / marqueur / automate cellulaire`、
  de `Agent / Raster / Marker / Hindernis`、ru `агент / змейка / клеточный автомат`）；
  法文引号按排版习惯改用 `« »`。
- **区域变体归一扩展**：除原有 `zh` 家族细分外，`fr-CA`、`de-AT`、`es-419`、`pt-BR`、`ko-KR`、
  `ru-RU` 等区域变体现在都能匹配到对应语言，不再回退英文。
- **兜底提示块支持多语言**（`#legacy-hint`）：脚本完全没能启动时（兼容模式 / 内核过旧 /
  ES Module 被拦截）显示的提示文案，改由一段**不依赖 ES Module 的内联经典脚本**按
  「上次手动选择 → 浏览器语言 → 英文」直接改写，覆盖全部 10 种语言；
  同时保留 `data-i18n` 标注，并把简体原文写回 `data-i18n-src` 作为查表依据，
  因此模块脚本正常启动、i18n 运行时接管后，切换语言仍能正确换语。

### 修复

- **单文件成品丢失兜底提示脚本**：打包器原先用 `/<script[\s\S]*?<\/script>/g` 清空 `<body>` 里的所有脚本，
  连不依赖模块的内联经典脚本一并删除，导致成品里 `#legacy-hint` 的兜底文案永远只剩简体中文。
  现改为只移除 `type="module"` 的入口脚本，内联经典脚本原样保留。
- **新语言包首尾空格丢失**：临时装配工具对译文做了 `trim()`，而部分键本身带首尾空格
  （如 `" 或 "`、`" · 抽样 1/{0}"`、`" 避撞触发后：立即停止运动。"`），
  翻译后会被拼成 `AouB` 一类的粘连文本。已改为原样保留译文（键的首尾空格形状不做裁剪）。

### 测试

- 语言包断言扩展到 10 种：`packs` 表、可选语言清单与语言名称串、具名导出检查（`export default`
  不被打包器识别）全部覆盖 `ko / fr / de / es / pt / ru`；`matchLocale` 新增 6 项区域变体断言。
- 新增回归断言：语言切换器双语标识（含 `for="lang-select"`）与 `.lang-tag` 靠右对齐样式、
  兜底提示块的三条 `data-i18n`、内嵌 `TEXT` 表覆盖 10 种语言、`data-i18n-src` 写回逻辑、
  抽取工具双侧登记同一份语言清单，以及**单文件产物保留内联兜底脚本**；
  断言总数 1795 → **1869**（语言包 10 × 1670 条）。
- 浏览器复验：10 种语言逐一切换，8 种非中文语言（日文按惯例豁免）应用界面**零汉字残留**，
  关键区域（顶栏副标题、配置面板标签与按钮、统计与日志面板、底部控制条）均为对应语言；
  语言标识与下拉框矩形不重叠（水平间隙约 14px）、点击标识后 `activeElement` 为下拉框、
  顶栏无横向滚动；兜底提示块在 en / ko / ru 下分别为英 / 韩 / 俄文，`data-i18n-src` 均为简体原文；
  控制台无脚本错误。

## v2.11.0

一次面向**界面语言**的功能拓展（配置版本 `CONFIG_VERSION` 保持 `1.5`，配置文件不新增字段）。
国际化层按系统语言自动匹配，语言选择结果只写入 `localStorage`（键 `gridsneaker:locale`），
**不进入配置、导出与分享链接**；对既有用户而言除界面语言本身外无行为变化。

### 新增

- **国际化（i18n）框架**（`src/i18n/`）：
  - 界面文案直接以**中文原文为键**（等价 gettext 的 msgid/msgstr），翻译发生在**渲染边界**——
    `src/ui/forms.js` 写入文本节点与 `title` / `placeholder` / `aria-label` 属性时统一过 `t()`，
    源码里上千处文案调用点无需逐个改键名；
  - 模板字面量按**位置编号**归一为占位符（`` `第 ${tick} 步` `` → `第 {0} 步`），
    译文可自由调整语序（英文写成 `Step {0}`）；
  - 运行期拼接的整句先精确查表，未命中再按带占位符的条目做**句式匹配**，
    捕获片段递归翻译，因此「日志：在前方产生 3 个障碍物」这类嵌套句子也能完整译出；
  - 不含中文的字符串直接放行，用户自定义命名不会被误翻。
- **四种界面语言**：简体中文（源语言）· 繁體中文 · English · 日本語；
  顶栏新增语言选择器，切换即时生效并同步 `<html lang>`。
- **系统语言自动匹配**：按 `navigator.languages` 顺序匹配，`zh` 家族按脚本 / 地区细分
  （`zh-Hant`、`zh-TW`、`zh-HK`、`zh-MO` → 繁體中文，其余 → 简体中文），
  匹配不到时回退 English；**手动选择优先**于系统语言并持久化在 `localStorage`。
- **静态骨架文案本地化**：`index.html` 中顶栏副标题等 4 条静态文案以 `data-i18n` /
  `data-i18n-attr` 标注，原文记入 `dataset`，反复应用不累积。
- **骨架句式**（`SKELETON_PATTERNS`）：`{0}（{1}）`、`{0}：{1}`、`{0}：{1} → {2}` 三条纯标点条目，
  专门把运行期拼出来的片段（各段已各自译好）用**目标语言的标点与语序**重新连接。
- **语言包抽取与校验工具**（`tools/i18n-extract.mjs`）：扫描 `src/**` 字符串字面量与
  `index.html` 标注文案生成 zh-CN 规范键集；`--check` 模式比对各语言包的缺译 / 多译 / 空值，
  已接入 `npm test`（`npm run i18n:extract` / `npm run i18n:check`）。
- **RTL 预留**：命中 `RTL_LOCALES` 的语言自动设置 `<html dir="rtl">`；
  阿拉伯语等语言本轮未收录，镜像版式适配属于后续工作。

### 修复

- **运行期拼接串恒为源码语言**（漏译）：原先这类字符串整体落在语言包之外——
  例如结束原因写成 `` `${END_LABELS.x}（${y}）` ``，归一键 `（{0}）` 不含实义文字被抽取规则丢弃，
  导致「达到步数上限（300）」永远显示中文。现分别改为「各段独立翻译后再拼接」或
  「含中文的完整字面量模板」（运行期字符串不变，仅让抽取脚本能登记条目），
  共修 `src/ui/app.js` 14 处、`src/core/simulation.js` 7 处，覆盖结束原因标签、得分明细分项、
  计分板地图规模、排行榜条目、日志副标题、避撞状态、参数摘要、规则集导入提示、格子编辑器读数等。

### 测试

- 新增国际化专项断言：语言匹配与回退、手动选择优先、`<html lang>` 同步、静态骨架文案本地化、
  占位符按位置回填与语序、句式匹配与递归翻译、多行文本逐行翻译、骨架句式，
  以及**四种语言包键集与源码文案的双向一致性**；断言总数 1701 → 1795。
- 浏览器复验：英 / 日 / 繁三种语言下逐项核对原漏译点（皮肤说明、概率行、避撞行、状态 chips、
  统计结束原因、日志、得分明细弹窗等），应用界面**漏译清零**，无控制台报错、无横向滚动、无文字裁切。

## v2.10.0

一次面向**轨迹观察与提示可读性**的功能拓展（配置版本 `CONFIG_VERSION` 保持 `1.5`）。
新增的三项可视化能力**默认全部关闭**，因此不改变任何既有场景的外观；
唯一的默认行为变化是「单蛇提示精简」（默认开启），它只删去一条与「轨迹」行完全同值的重复信息。

### 新增

- **轨迹方向箭头**（`style.showTrailArrows` / `trailArrowSpacing` / `trailArrowScale` / `trailArrowColor`）：
  沿**已经播放到**的轨迹按间隔绘制指向前进方向的实心三角，长轨迹上不必再靠「头部位置」推断走向。
  - 间隔按**像素距离**累计（而非按轨迹点计数），因此转弯与直线上的疏密一致，默认 3 格；
  - 箭头亮度复用轨迹的渐隐 / 热点过滤解算，随播放进度自然变暗，与轨迹共用网格区域裁剪；
  - 按「连续段」逐段累计，跨边界处每段独立起算，**不会在留白区留下指错方向的箭头**；
  - 依附于「轨迹」显示：关闭轨迹后箭头一并隐藏；
  - 面板位置：「展示样式 → 高级视觉特效 → 轨迹方向箭头」（开关 + 间隔 / 尺寸 / 颜色）。
- **本步变化高亮**（`style.showStepDiff` / `stepDiffAlpha` / `stepDiffColor`）：
  把「当前步相对上一步**环境状态发生变化**」的格子填充高亮出来——元胞自动机的出生 / 死亡、
  标记物的生成与消耗、障碍物增删都能一眼看到，适合逐帧观察环境演进。
  - 比较结果按帧下标缓存（同一帧被反复重绘时不再重复遍历两份环境数组）；
  - 帧按采样步长存储，长跑降采样时高亮的是「两帧之间的净变化」，已在方法注释中明确；
  - 面板位置：「展示样式 → 高级视觉特效 → 本步变化高亮」（开关 + 不透明度 / 颜色）。
- **热点轨迹过滤**（`style.trailHotOnly` / `trailHotMin`）：
  只绘制「该格**整轮经过次数** ≥ 阈值」的轨迹段，让人一眼看出反复踩踏的热点路径，默认关闭。
  - 实现方式是**按阈值把被过滤的点在连续段上切成断点**，而不是「跳过某些点」：
    于是直线 / 切角 / 曲线三种连接方式与「按经过次数 / 按经过次序」两种颜色分级同时生效，
    且**不会跨过被过滤的区间连线**；
  - 经过次数取整轮口径（与「按经过次数取色」一致），过滤结果稳定、不随播放进度反复显隐；
  - 掩码按「阈值 + 轨迹长度」缓存，轨迹更新时失效重建；关闭开关时不构建掩码（零额外开销）；
  - 面板位置：「展示样式 → 高级视觉特效 → 热点轨迹过滤」（开关 + 热点判定次数）。

### 改进

- **单蛇场景的悬浮提示精简**（`style.hoverTipSlimSingleSnake`，默认开启）：
  检测画面中的移动体数量，**只有一条移动体**时「轨迹」行的「共 N 次」就是整轮累计，
  再列一条同值的「历史累计经过次数」纯属重复信息，因此自动省略该行，
  只保留随播放进度变化的「当前路径经过次数」；多移动体场景两条口径并存（避免聚合歧义）。
  可关闭开关回退到旧版完整字段。
- **「提示内容」的说明文案**同步交代单蛇精简行为，设置面板与渲染层口径保持一致。

### 修复

- **切换轨迹颜色分级后色带不刷新**（`fadeStyleTable` 缓存键缺项）：
  「按经过次数」与「按经过次序」两种分级的**量化档数相同**（都是 `FADE_BUCKETS = 16`），
  而缓存键只比较了档数，于是切换分级后仍沿用旧色带，
  必须等主题 / 线宽 / 渐隐开关变化才刷新。现将色带（`ramp`）纳入缓存键比较，切换即时生效。

### 测试

- 新增「轨迹方向箭头 / 本步变化高亮 / 热点轨迹过滤 / 色带缓存键 / 单蛇提示精简」五组断言，
  含渲染层**调用级**校验（箭头数量随间隔变化、变化格填充数与环境差异格数一致、
  热点掩码与保留点数精确对齐）、`STYLE_DEFAULTS` 与配置层**默认值双向一致性**校验，
  以及新增配置项在**统一配置面板**与**差异比对中文标签**中的源码级回归；断言总数 1621 → 1701。

## v2.9.0

一次面向**元胞自动机设置模块**的重构与功能拓展（配置版本 `CONFIG_VERSION` → `1.5`）。
新增能力默认**零行为变化**：`cellEditor.placeMode` 默认仍为 `initial`（与旧版把格子写入第 0 步一致），
`cellEditor.timedPatches` 默认为空数组；子选项卡只改变配置项的归属，控件与取值完全保留。

### 新增

- **元胞自动机设置面板重构为五页子选项卡**：原先把十余个配置项平铺在同一个折叠分组里，
  规则表、初始环境、交互与模板互相穿插，想调某一项往往要来回滚动。现按用户配置时的思考顺序收敛为
  **核心规则 / 演化规则 / 初始环境 / 运行与性能 / 交互与模板**：
  - `核心规则`：启用开关、环境状态集合、邻域与半径、边界条件、更新顺序；
  - `演化规则`：状态转移规则表 + 规则集导入 / 导出；
  - `初始环境`：第 0 步环境的生成方式（全空 / 随机散布 / 图案文本）+ 环境模板；
  - `运行与性能`：与环境同步方式、演化间隔、稳定即收尾，并给出性能口径提示；
  - `交互与模板`：标记物交互机制、快捷模板、模拟参数预设。

  「启用元胞自动机」开关与「第 0 步缺少初始环境」警告条固定在子选项卡之上，任何子页都可见；
  活动子页存于 `state.caSubTab`，面板重建后仍停留在原页。子选项卡视觉上比主选项卡更轻
  （更小字号与内边距、不用阴影），配色复用同一套 `--tab-*` 变量，因此**色彩主题配置对子选项卡同样生效**。
  配置搜索时全部子页自动展开并标注子页名称（`.cfg-sub-tabs.searching`），
  命中项不会藏在未激活的子页里。
- **「第 0 步缺少初始环境」友好警告**（`caEditorEnvWarning()` / `caEditorEnvAlert()`）：
  编辑器已启用、且第 0 步既没有手工绘制的格子、元胞自动机的初始状态也等价于「全空」
  （随机散布密度为 0、图案文本为空白同样按全空处理）时，面板顶部出现醒目提示条
  「第 0 步还没有初始环境」，说明「这样运行后画面上不会出现任何环境单元，很难观测到模拟的直观变化」，
  并给出可操作指引；已存在延迟放置时还会点明「当前只有 N 格延迟放置补丁（最早在第 X 步生效），
  第 0 步仍是全空」。提示条自带**「去配置初始环境」**按钮，一键切到「初始环境」子页。
- **延迟放置（`cellEditor.timedPatches`）**：可在**当前任意模拟步数**放置环境。
  放置时机由 `cellEditor.placeMode` 控制（`initial` 写入第 0 步 / `step` 写入当前步）：
  - 补丁按步号索引，**只在匹配步「推进之前」就地写入世界**，因此早于该步的历史帧保持原样，
    **不回溯改写已完成的模拟进程**；步号 ≤ 0 的补丁等价于初始环境，与手工绘制一并在主循环之前写入；
  - 写入时置 `cellsDirty`，环境计数与帧快照随之刷新；同时写入运行日志
    （`trigger: 'timedPlace'`，文案注明「此前帧不受影响」），便于回溯；
  - 放置会触发整轮重算，`state.pendingTick` 让重算完成后**自动跳回放置所在步**，
    保持「就在当前步放置」的操作手感。
  编辑动作（增 / 删 / 拖拽连画 / 随机散布 / 清空 / 撤销重做）全部经由统一的「放置目标」抽象
  （`editorTarget()`）落到当前目标；撤销快照同时覆盖 `painted` 与 `timedPatches`，
  撤销不会丢掉延迟放置。面板新增「放置到」下拉与实时读数「本次点击将写入第 N 步：
  只影响该步及其之后的帧，此前已完成的帧保持原样（不回溯）」，计数行同时展示
  「已绘制 N 格 · 延迟放置 M 格（K 个步）」。
  另提供「清除本步放置」与「清空全部延迟放置」两个清理入口（破坏性操作先经确认）。
- **规则集导入 / 导出**（`exportCaRuleset` / `parseCaRuleset`）：导出的 JSON 只含状态转移规则表
  （附带类型标识 `gridsneaker:ca-rules`、版本号与状态名清单），**不夹带移动体 / 规则 / 外观配置**，
  因此可跨场景、跨用户直接传递。面板提供「复制规则集」「下载规则集」，以及
  「覆盖导入 / 追加导入」两种导入方式；导入的规则与配置内的规则走同一条规范化路径
  （`normalizeCaRules`），结构永远一致。规则引用了当前环境状态集合中不存在的状态时给出中文警告，
  JSON 非法 / 缺规则表 / 规则表为空时抛出可读错误，界面据此 toast 而不崩溃。
- **环境模板保存与复用**（`buildEnvironmentTemplate` / `parseEnvironmentTemplate`）：
  模板 ＝ 环境状态集合 + 初始状态 + 手工绘制的格子 + 网格尺寸，**不含移动体 / 规则 / 外观**，
  因此可跨场景复用。支持命名保存到浏览器本地、一键载入、删除、导出当前环境、粘贴导入模板；
  载入会整体替换「状态集合 / 初始状态 / 手绘格子」，并明确提示替换范围与
  「模板网格尺寸与当前不同，超出当前网格的格子已被忽略」（不静默丢格子）；
  状态集合被替换后若原有规则引用了已不存在的状态，复用规则集解析器给出同口径提示。
- **模拟参数预设**：只保存演化参数（邻域 / 半径 / 边界 / 更新顺序 / 同步方式 / 演化间隔 /
  稳定判定 / 初始状态），**不含规则表与手绘环境**，因此可以在同一套环境上快速切换参数组合、
  观察同一初始局面的不同演化结果；载入时回显生效项数。
- 环境模板与参数预设共用一套「本地命名记录」管理块（`namedStoreSection`），
  分别使用独立存储键 `gridsneaker:env-templates` 与 `gridsneaker:ca-presets`，
  条数上限 30 条；保存 / 删除**就地刷新列表**，不触发整轮重算。

### 变更

- `CONFIG_VERSION` 由 `1.4` 升级到 `1.5`：新增 `cellEditor.placeMode`（白名单 `['initial', 'step']`，
  非法值回退 `initial`）与 `cellEditor.timedPatches`（`[{ tick, col, row, state }]`）。
  延迟放置按「坐标须落在网格内、状态名须真实存在且非 `empty`、步号非负」逐项校验，
  同一 `(步数, 坐标)` 只保留最后一次录入，并按 `MAX_TIMED_PATCHES = 2000` 截断，
  因此**规范化保持幂等**（JSON 往返、分享链接往返结果恒定）。
- 配置差异比对补充中文标签与取值文案：`放置方式`、`延迟放置补丁`，
  以及 `cellEditor.placeMode` 的枚举文案；`CELL_PLACE_MODE_LABELS` 由差异模块显式引入，不硬编码字符串。
- 「清空手绘」在「当前步放置」模式下仍**只清初始环境**（`editorTarget('initial')`），
  不会误删更晚步数的延迟放置。

### 修复

- **`clearStepPatch` / `clearAllTimedPatches` 曾是悬空实现**：两个清理函数已接入编辑器面板
  （在「当前步放置」模式或存在补丁时出现），不再是无入口的死代码。

### 测试

- 新增 5 个测试段落共 146 项断言：
  - **行为级**：延迟放置只在匹配步生效、第 0 步历史帧不被回溯改写、步号 ≤ 0 等价初始环境、
    运行日志可回溯；`timedPatches` 的剔除 / 去重 / 幂等 / 分享往返与 `placeMode` 规范化；
    规则集导出内容边界与导入往返无损（含状态不匹配警告与三类可读错误）；
    环境模板往返一致与越界 / 重复 / 空状态数据清洗。
  - **源码级**：CA 五页子选项卡的分类归属、容器与样式、搜索联动；警告判定与「去配置初始环境」跳转；
    `editorTarget` 抽象覆盖全部编辑入口、`pendingTick` 回跳；规则集 / 环境模板 / 参数预设三处面板与本地命名记录。
- 同步修正 1 项既有断言：视觉模块旧子选项卡的「不残留死样式」校验不再把
  `data-sub-tab-label` 列为禁用（该属性现由元胞自动机子选项卡合法复用）。
- 测试总数 1475 → **1621 项全部通过**。

## v2.8.0

一次「交互细节 + 可用性」打磨（配置版本 `CONFIG_VERSION` → `1.4`）。
新增能力**默认零行为变化**：默认的避撞处理方式与旧版逐位一致，
「拖拽连画」默认开启仅影响「格子编辑器已启用」时的操作手感。

### 新增

- **画布格子编辑器新增「元胞自动机状态」工具**（`cellEditor.tool = 'state'`，
  目标状态存于 `cellEditor.stateName`）：修复生命游戏等模板（状态集只有 `empty` / `alive`）
  下**无法手工放置「存活」格**的问题。面板新增「目标状态」下拉，可显式选择任意非空环境状态；
  选「自动」时取状态集合中的首个非空状态（生命游戏模板即 `alive` ＝「存活」）。
  同时给出「本次点击将放置：…」的实时读数，把「自动」的解析结果摊开，消除选择歧义。
  目标状态按白名单校验（必须真实存在且非 `empty`），非法值回退为「自动」。
- **「避撞触发后的处理」选项**（`safety.onAvoid`，默认 `'turn'`）：安全避撞在方向选择阶段
  剔除不安全候选，当本步本应朝向的方向被剔除时，此前只能自动改道；现可二选一：
  - `'turn'`（默认）**自动切换其他可行方向继续运动** —— 与旧版行为完全一致；
  - `'stop'` **立即停止运动** —— 本步原地停止、不产生位移，计入「停止次数」，
    并遵循「撞到障碍物」的结束规则与生命机制。

  `'turn'` 完全不进入新分支，随机序列与旧版逐位一致，不产生任何行为漂移。

### 变更

- **「拖拽连画」默认开启**（`cellEditor.drag` 默认 `true`）：编辑器启用后按住左键拖动即可连续
  绘制，连续编辑无需再额外勾选；起手格已放置则本轮为连擦，否则为连画，整轮拖拽只记一条撤销历史。
  仍可在面板中关闭。旧版本（`1.3` 及更早）保存的配置在载入时一并升级为开启，
  并在迁移说明中提示——该开关只影响鼠标拖拽手感，随时可再次关闭。
- **悬浮提示的经过次数改用专业表述**：`本次经过` / `上一次经过` 调整为
  `当前路径经过次数：第 N 次（最近一次经过：第 M 步）` 与
  `历史累计经过次数：共 K 次（上次经过：第 M 步）`。前者是「截至当前播放进度」的计数
  （随播放增长），后者是整轮累计（最终结果），两个口径分开陈述，不再混用。
- **速度调节独占一行**：`速度` 滑杆与「步 / 秒」读数从「循环 / 自动运行 / 跟随 / 自适应速度」
  那一行拆出，避免窄屏下换行位置不可控导致滑杆被压缩、读数被裁切。
- **配置差异比对补充中文标签**：`避撞触发后的处理`（含「立即停止运动」/
  「自动切换其他可行方向继续运动」两种取值文案）与 `目标状态`。
- `CONFIG_VERSION` 由 `1.3` 升级到 `1.4`（新增 `safety.onAvoid`、`cellEditor.stateName`，
  并将 `cellEditor.drag` 的默认值由关闭改为开启），`normalizeConfig` 为两个新字段补齐默认值与收敛规则，
  并保持**规范化幂等**（JSON 往返、分享链接往返结果恒定）。

### 修复

- **格子编辑器的「标记物工具」无法表达「存活」**：生命游戏模板下标记物工具的兜底语义
  落不到一个语义明确的状态上。现由独立的「元胞自动机状态」工具承担该职责，
  目标状态可显式指定、也可自动取首个非空状态。
- **悬停提示中「本格首次经过」的计数口径不自洽**：原文案 `上一次经过：无（本格首次经过）`
  与同段的计数描述混用两套口径，现统一并入 `历史累计经过次数` 一并陈述。

### 交互细节

- 搜索栏「清除」按钮接入统一术语浮层：悬停即说明「清除当前搜索框内的全部输入内容，
  并还原搜索前的分组折叠状态；仅影响搜索过滤结果，不会改动任何已保存的设置项」；
  同时保留原生 `title` 作为无 JS / 触控端兜底。「展开全部 / 收起全部」也补上 `title`。
- 搜索框内按 `Esc` 即可一键清空（与点击「清除」走同一条链路），无需把光标移到按钮上。
- 格子编辑器开启时画布光标切换为十字准星（`#canvas.editing`），
  一眼可知「点击画布 = 增删格子」已接管。
- 「清空手绘」改为破坏性操作确认对话框（默认聚焦「取消」），并提示「可通过撤销回退」。

### 测试

- 新增 3 个测试段落共 53 项断言：手工放置「存活」格并写入初始世界（含状态集不含该状态时的剔除）、
  「避撞触发后的处理」的规范化 / 幂等 / 分享往返与行为差异（`'stop'` 全程原地不动且计入停止次数）、
  以及本轮交互 / 布局改动的源码级回归。
- 同步更新既有断言：`CELL_TOOLS` 由 3 种扩为 4 种；悬浮提示文案由
  `本次经过 / 上一次经过` 改为 `当前路径经过次数 / 历史累计经过次数`。
- 测试总数 1422 → **1475 项全部通过**。

## v2.7.0

面向「高度自定义化」定位的一次体验升级（配置版本 `CONFIG_VERSION` → `1.3`）。
所有新增能力**默认关闭 / 默认零行为变化**，旧配置与旧分享链接的行为完全不变。

### 新增

- **画布格子编辑器**（`cellEditor`，默认关闭）：在画布上直接增删格子元素。
  左键单击添加、再次单击删除，障碍物与各类标记物通用；点击坐标一律经 `Renderer.hitTest`
  反查行列号，与绘制几何共用同一套换算（无交互偏移）。配套能力：
  拖拽连画、右键擦除、画笔尺寸（1~9 格）、随机放置（按概率从随机池抽类型）、
  按密度一键随机散布、撤销 / 重做（上限可调，`Ctrl/Cmd + Z` / `Ctrl/Cmd + Shift + Z` / `Ctrl/Cmd + Y`）
  与实时已绘制格子计数。`painted` 随配置保存与分享，并在模拟开始时写入世界，
  等价于一份「手工绘制的初始环境」。
- **自定义标记物类型**（`markerTypes`，默认空列表）：为交互标记物新增「类型层」——
  同一状态可挂多个类型，各自带外观样式（颜色 / 符号 / 绘制方式）、
  生效条件（始终 / 概率 / 长度上下限 / 步数门槛）、触发权重与内置反馈（增量 / 指定值 / 百分比、
  概率、是否消耗、消耗后状态、反馈色）。运行时先按条件过滤、再按权重抽取一个生效类型，
  类型自带 `effect` 时覆盖反馈规则表，否则沿用原 `markerInteraction.effects`。
  `markerTypesForState` / `markerConditionMet` 是运行期唯一入口，后续新增标记物只需追加一条记录。
- **障碍物陷阱**（`obstacleTypes[].trap`，默认关闭）：自定义障碍物类型可附加陷阱属性，
  含**触发概率**与**死亡概率**两级可调参数（1 = 必定、0 = 从不）。蛇尝试移动到陷阱格时，
  先按触发概率判定是否触发，触发后再按死亡概率判定是否致命；未致命或未触发都退回普通障碍物阻塞。
  每次判定（触发 / 未触发 / 致命 / 未致命）都写入运行日志（`trap.log`，可关闭），
  便于核对参数是否符合预期；新增统计项「陷阱触发」与「陷阱死亡」。
- **障碍物交互式放置**：一是「先选自定义障碍物类型，再点击格子精准放置」；
  二是「开启随机放置后按设定概率从自定义障碍物池中抽取类型」；两种模式均支持再次点击删除。
- **「停止」移动选项**（`moveRules.stop`，默认权重 0，故默认行为不变）：方向权重面板由
  「左 / 直 / 右」扩为「左 / 直 / 右 / 停止」四项，条件概率规则同样支持。
  被选中时不产生任何有效位移，判定链与「尝试移动到障碍物」完全一致——计入碰撞统计、
  写入 obstacle 事件与碰撞高亮，并遵循「结束规则 → 撞到障碍物」与生命机制；
  新增统计项「停止次数」。全部权重为 0 时仍排除「停止」，保持旧版「总会朝某个方向移动」的兜底语义。
- **交互标记物说明浮层**：标记物状态 chips 右侧新增「?」入口（`.chip-help`），
  悬停弹出说明面板，逐条解释该标记物的**功能 / 生效规则 / 使用场景**；
  面板复用既有浮层节点与定位逻辑（随视口自动避让），不遮挡核心操作区域，
  自定义类型名称先经 `escHtml` 转义再写入，避免破坏浮层结构。
- **陷阱格可视化**（`style.showTraps` / `style.trapColor`）：为启用了陷阱的障碍物格子叠加
  危险纹理与外框，一眼即可分辨普通障碍物与陷阱；悬浮提示同步给出陷阱参数。
- **配置差异比对扩展**：新增配置分组「自定义标记物类型 / 自定义障碍物类型 / 画布格子编辑器」
  与「停止权重 / 陷阱属性 / 触发概率 / 死亡概率 / 画笔尺寸」等字段的中文标签，
  载入模板前的覆盖提示不再出现原始字段名。

### 变更

- `CONFIG_VERSION` 由 `1.2` 升级到 `1.3`；`normalizeConfig` 为
  `moveRules.stop`、`obstacleTypes`、`markerTypes`、`cellEditor`、`style.showTraps` 等字段补齐
  默认值与收敛规则（概率统一夹取到 `[0,1]`、画笔尺寸 `[1,9]`、类型数量上限 `MAX_MARKER_TYPES`
  / `MAX_OBSTACLE_TYPES`），并保持**规范化幂等**（JSON 往返、分享链接往返结果恒定）。
- `DEFAULT_HIDDEN_STATS` 收录新增的低频统计项（停止次数 / 陷阱触发 / 陷阱死亡），
  统计面板默认不拥挤，需要时可逐项开启。

### 修复

- **自定义标记物名称 / 符号会破坏说明浮层**：名称与符号来自用户输入，此前直接拼进浮层 HTML。
  现统一经 `escHtml` 转义，用户自定义内容不再可能穿透成标签。

### 测试

- 新增 5 个测试段落共 112 项断言：格子编辑器的规范化 / 幂等 / 分享往返、
  自定义标记物类型的规范化与条件判定、`painted` 写入初始世界（绘制的障碍物真的挡在蛇前）、
  陷阱两级概率的四种组合（致命 / 未致命 / 不触发）与日志落盘、
  「停止」的权重夹取与行为（位置恒定、计入碰撞、按「撞到障碍物」收尾）、
  以及界面 / 差异比对的源码级回归。测试总数 1310 → **1422 项全部通过**。

## v2.6.6

### 修复

- **「多蛇生态」模板的自撞逻辑与预设不符**：模板此前同时保留了「结束规则 → 撞到自身」与
  `safety` 的多项规避范围，蛇一旦压到自身身体就整轮终止，生态既跑不长、也没有个体被淘汰。
  现模板显式关闭 `endConditions.selfCollision`，并把安全避撞范围收敛为**仅「自身身体」**一项
  （`avoidBody: true`，其余四项关闭）：自撞只触发避撞、不再结束运行，同时保留了
  「障碍物 / 其它移动体 / 边界」这些真实碰撞，长跑中会自然出现个体死亡与数量波动。
- **新蛇方向与网格类型不一致（显示与行为不符）**：`normalizeConfig` 原先原样保留
  `spawn.direction`，在方形网格里选中的 `up` 切到六边形网格后会被 `parseDir` 静默解析为
  0 号方向，而下拉框因无匹配项显示为「随机」——面板显示与真实行为不一致。
  现新增 `normSpawnDirection(raw, gridType, fallback)`：不匹配当前网格类型的方向名一律回退为
  `random`，面板读数与生成行为重新一致。
- **「继续运行 +N」与「单次运行步数上限」语义脱节**：按钮文案与推进跨度都写死默认值，
  用户把单次运行长度调大后，继续运行仍按旧跨度推进，提示语也随之失真。
  现统一由 `continueStep()` 给出跨度（跟随 `cfg.frameCap`，收敛到上限、缺省回退默认值），
  按钮文案、推进步数与面板提示语三处保持一致。

### 变更

- **两条生成路径共用同一套数量口径**：环境规则「生成新移动体」原先上限写死 32，
  与多蛇系统的上限（`MAX_AGENT_SLOTS = 64`）不一致。现统一引用同一常量，
  并让两条路径都按**生成序号**写入 `safetySlot`，画布标签（`蛇n`）与逐条开关一一对应。

### 新增

- **逐蛇独立安全避撞**：多蛇专属设置模块新增「逐蛇安全避撞」折叠分组，
  可按**主移动体 / 蛇1 / 蛇2 ……** 逐条开启或关闭安全避撞，并支持「＋ 增加一条蛇」
  「－ 减少一条蛇」「跟随默认」整组操作，分组徽标显示已单独设置的条数。
  配置层为 `multiSnake.safety = { default, perAgent }`：`perAgent[i]` 为 `null` 时跟随
  `default`，逐条设置与默认值都可随分享链接往返；`agentSafetyEnabled(cfg, slot)` 是唯一判定入口，
  未启用多蛇系统时恒为「开启」，与既有配置完全兼容。
- **生成总数上限 `spawn.maxTotal`**（默认 0 = 不限制）：与「最大同时存在」配合，
  避免长时间运行时蛇数量无限累积；`0` 以外的取值在整轮累计生成数达上限后停止生成。
- **单次运行步数上限 `frameCap`**（默认 20000，上限 200000）：未勾选「结束规则 → 达到步数上限」时，
  一轮运行在本上限处停止，可用控制条「继续运行」逐级推进。该值是可保存、可分享的真实配置项，
  而非模拟器里的隐藏兜底；默认值与上限值统一在 `config.js` 定义，`simulation.js` 只做再导出，
  避免「配置默认值」与「模拟器兜底值」漂移。
- **「场景与运行」选项卡补充内容**（原先仅一个分组，内容单薄）：新增
  **运行控制**（模拟帧率、单次运行步数上限，并按「是否启用达到步数上限」动态说明本上限的作用）与
  **场景边界与规模**（网格宽高、边界行为、多蛇系统开关、最大同时存在、生成总数上限，
  并显示网格总格数）两个分组，选项卡提示语同步更新。

### 测试

- 新增「逐蛇安全避撞：配置层与逐条判定入口」：默认值、`normalizeConfig` 的各类非法输入收敛
  （含超长数组截断到 `MAX_AGENT_SLOTS`）、`agentSafetyEnabled` 的 7 条判定（含
  「未启用多蛇系统恒为开启」的向后兼容）、以及「逐条关闭时 `filterSafeOptions` 返回 `null` /
  逐条开启时正常筛选，同一配置里两种行为并存」的行为级验证。
- 新增「多蛇：生成序号即逐蛇开关下标 / 整轮生成总数上限（行为级）」：手工构造上下文调用
  `maybeSpawn`，验证 `stats.spawns`、各 `Agent.safetySlot`（主移动体恒为 0）、
  达 `maxTotal` 后停止生成、`maxTotal: 0` 时持续生成到 `maxAgents`；
  以及 `Agent` 的 `safetySlot` 在 `clone()` 后保持、负值收敛、缺省为 `null`。
- 新增「新蛇方向与网格类型的一致性 / 单次运行步数上限」：方形 → 六边形的方向回退、
  `frameCap` 的边界收敛，以及 `endReason.code === 'frameLimit'`、`stats.steps <= frameCap`、
  显式传入上限优先级更高的行为级验证。
- 新增「逐蛇安全避撞 UI 与「场景与运行」选项卡扩充（源码级回归）」：逐条开关的生成规则、
  三个操作按钮、分组徽标、`maxTotal` 输入、两个新分组与装配位置、选项卡提示语、
  差异比对中文标签，以及新增字段的分享链接往返；并锁定「继续运行」跨度跟随配置
  （不再写死默认值、按钮文案与提示语同步）。
- 当前共 **1307 项断言全部通过**。

## v2.6.5

### 修复

- **环境规则产生的障碍物 / 标记物在回放与导出中不可见**：规则动作（产生 / 移除障碍物与标记物、
  修改环境状态、清空某状态、绘制轨迹状态）调用 `World.set` 后从不置 `ctx.cellsDirty`，
  帧快照 `storedCells` 因此一直停留在旧内容——环境确实被改写、统计也对，但画面与导出数据里看不到。
  现 5 处环境写入（含直接改写 `cells` 数组的「清空某状态」，仅在真的清掉格子时）统一置脏，
  回放帧、导出数据与统计口径三者重新一致。
- **「避开全部」总开关开启时不联动子项，且会误清空已勾选项**：`chkBind()`（`forms.js`）调用回调时
  未回传新值，安全避撞的总开关与子项处理函数依赖该值，实际收到 `undefined`，
  于是「开启」被当成「关闭」处理（四项被写成 `undefined`）。现 `chkBind` 把新值一并传出，
  总开关双向联动、取消任一子项自动关闭总开关、重新勾选子项不会反向打开总开关。

### 优化

- **环境计数增量化**：`obstacleCount` / `markerCount` 原先每步对整张网格做一次全表扫描
  （200×200 网格跑 2 万步约 16 亿次比较），现只在格子真被改写（`ctx.cellsDirty`）时重算，
  并与帧快照刷新共用同一次判定；进入主循环前先算好首帧计数。
- **播放合并重绘**：一个动画帧内可能推进多步（高倍速 / 掉帧追帧），原先每推进一步都执行一次
  `draw + updateControls + highlightLogs`，中间帧的结果会被立即覆盖。现只记录「是否推进过」，
  循环结束后统一刷新一次 DOM。
- **移动前状态按需拷贝**：`agent.prevState` 每步深拷贝整条蛇身，但只有「排斥」交互回退这一步时
  才会用到。现仅在该模式下备份，长蛇场景每步省下一次全身体节的对象分配。
- **配置搜索防抖**：搜索过滤需要对每个分组做一次 `textContent` 序列化，逐键触发在大面板上很卡。
  现按输入停顿（120ms）合并为一次过滤，输入框本身仍即时回显；「清除」按钮取消待执行的防抖并立即还原。
- **长跑运行反馈**：大网格 + 高步数（或开启元胞自动机）时模拟会同步占满主线程，界面此前完全无反馈。
  现按 `步数 ×（8 + 开启 CA 时的网格格数）` 预估计算量，超过阈值时先画出控制条上的
  「◌ 正在运行模拟（最多 N 步）…」提示，再把计算推到下一个事件循环；期间若又发起新计算，
  旧的一次按运行序号作废，不会用过期结果覆盖新结果。

### 变更

- **视觉显示层级收敛**：移除「展示样式」下的二级子选项卡层（`VISUAL_SUBTABS` / `.cfg-sub-*`），
  四类视觉设置改为与其它类别完全一致的平级折叠分组（基础视觉设置 / 高级视觉特效 / 悬停与提示 /
  色彩主题配置），层级统一为「主选项卡 → 折叠分组 → 字段」，不再有第三层容器。
- **安全避撞默认值**：`safety.avoidAll` 默认开启（连同自身身体 / 障碍物 / 其它移动体 / 边界四项）；
  边界行为为「不可穿越」（停止 / 反弹 / 随机转向 / 自定义）时自动补开 `avoidWall`，
  切回「穿越到另一侧」不再自动开启；用户显式关闭的选择始终优先，所有控件均可手动改回。

### 新增

- **统计术语说明（Tooltip）**：新增 `STAT_TERMS` 术语表，覆盖全部统计项（含长度曲线 / 转向分布），
  逐项给出统计口径说明。统计面板的统计格与「统计项显示配置」的复选框文字均带虚线下划线 +
  `cursor: help`，鼠标悬停即复用 `#tooltip` 浮层显示「术语名 + 口径说明」（单独放宽浮层最大宽度，
  并沿用边界翻转避免溢出视口）。

### 测试

- 新增「规则动作改写环境后：回放帧快照必须同步刷新（cellsDirty）」行为回归：直接验证规则产生的
  障碍物 / 标记物出现在帧快照中、帧快照内障碍物数量单调不减、「清空某状态」在快照中生效。
- 新增「任务5 优化」源码级 + 行为级回归：环境计数与帧快照共用脏判定、按需备份移动前状态
  （并在小网格塞满移动体的场景下验证排斥仍会回退）、播放循环内每帧只刷新一次 DOM、
  搜索防抖与「清除」的即时还原、长跑提示的出现 / 作废 / 收起；以及「每一帧的环境计数统计
  与其画面快照逐帧自洽」的不变量断言。
- 扩充安全避撞 UI 回归：锁定 `chkBind` 必须把新值回传给回调（总开关联动依赖该值）。
- 当前共 **1234 项断言全部通过**。

## v2.6.4

### 新增

- **视觉显示拆分为子选项卡**：原先「展示样式」类别下堆叠了全部视觉选项，条目过多。
  现按功能归入四个子选项卡，每个子选项卡内再按功能细分为折叠分组：
  **基础视觉设置**（格子绘制 · 显示内容 · 环境显示 · 连接方式）、
  **高级视觉特效**（轨迹渐隐与配色 · 尖端平滑 · 交互特效 · 发光）、
  **悬停与提示**（重访高亮 · 行列准线 · 悬浮提示内容 · 穿越日志）、
  **色彩主题配置**（选项卡配色 · 紧凑排版 · 统计动画 · 界面配置）。
  子选项卡选择会被记忆（面板重建后保持）；搜索设置项时自动展开全部子选项卡（`.cfg-tabs.searching`），
  命中项不会藏在未选中的子选项卡里。
- **统计项显示配置**：生成新蛇、移动体消失、融合次数、排斥次数、生命机制（当前 / 上限 / 增减 /
  重生 / 耗尽 / 全部转化）等**低频统计项默认隐藏**（`style.hiddenStats`，默认集合
  `DEFAULT_HIDDEN_STATS`），只保留步数、帧位、耗时、结束原因、长度、覆盖率、碰撞、移动体数、
  规则触发等高频核心读数。隐藏项在「统计模块 → 统计项显示配置」中按功能分组逐项勾选开启，
  并提供「仅核心指标 / 全部显示 / 全部隐藏 / 恢复默认」整组预设与「当前显示 N / M 项」计数提示。
  隐藏只影响统计格与摘要行的生成，统计与导出数据不受影响；隐藏集合按统计项固定顺序落盘，配置差异稳定。
- **选项卡配色自定义**（`style.tabColors`，6 个色槽）：已激活 / 未激活两态各有背景色、文字色、
  边框色，可在「色彩主题配置」中逐项调整（取色器 + 十六进制文本输入双向同步，非法值即时提示并回滚），
  面板内附「已激活 / 未激活」实时预览与「重置为默认配色」；配色写入根元素 CSS 自定义属性
  （`--tab-on-bg` / `--tab-off-bg` 等），主选项卡、视觉子选项卡与统计口径切换共用同一套变量。
- **紧凑排版 `style.compact`**（默认关闭）：在 `body` 上挂 `.compact` 类，统一压缩分组、字段、
  开关行、统计格与工具条的间距，适合小屏或希望一屏看到更多设置项时。
- **统计切换淡入 `style.statFlash`**（默认开启）：切换实时 / 总计统计口径时数值做一次淡入过渡，
  低性能设备可关闭以减少重绘。
- **分组一键展开 / 收起**：配置面板与控制条顶部新增「展开全部 / 收起全部」（`setAllGroupsOpen`），
  无需逐组点击。

### 优化

- **核心规则开关排版统一**：新增 `switchField()` 组件（`forms.js`）与 `.switch-field` 样式，
  采用「标签左 / 控件右」两列网格 + 说明文本独占整行的结构，固定行高（`min-height: 30px`）
  保证所有开关、标签与说明落在同一基线上。核心规则的「蛇形实体开关」、长度策略的「启用」、
  多蛇系统的「启用多蛇系统」、「重访格高亮」「悬停行列准线」「悬浮提示」「穿越事件日志」等
  原先排版参差的开关行统一改用该组件。
- **选项卡两态色差增强**：未激活态压暗降饱和、激活态高亮配纯白文字并加粗（`font-weight: 600`）
  与 inset 边框 + 投影，已激活 / 未激活辨识度明显提高；`.mode-switch` 的统计口径按钮一并收敛到同一套变量。
- 配置差异比对（`config-diff.js`）补齐新增字段的中文标签（隐藏的统计项 / 选项卡配色 / 紧凑排版 /
  统计切换淡入及 6 个色槽），载入模板前的改动提示保持可读。

### 测试

- 新增源码级回归分组「视觉子选项卡 / 统计项显示配置 / 开关行排版 / 选项卡配色（源码级回归）」，
  覆盖：默认隐藏集合的内容与规范化（含旧存档缺字段回退、去重、非法形态回退、空集合分享链接往返）、
  四类视觉配置的子选项卡归属与搜索态展开、`switchField` 的存在与对齐规则、6 个色槽的默认值与
  非法色值回退、`applyTabTheme` 的变量写入与重建时机、紧凑排版 / 统计淡入开关、分组一键展开收起、
  配置差异比对新增标签等。
  当前共 **1057 项断言全部通过**。

## v2.6.3

### 新增

- **边界进出点独立显示**：原先边界进出点标记只在「轨迹」开启时才绘制（`showTrail && showCrossings`），
  现已与轨迹主体**完全解耦**，由独立开关 `style.showCrossings` 控制并**默认关闭**。
  关闭轨迹后仍可按自身开关单独显示进出点；此时渲染器走 `syncTrailCursor()` 单独推进轨迹上界，
  标记依旧随播放进度依次出现，不会一次性全部铺满。
- **进出点标记尺寸 `crossingScale`**（0.4 ~ 3）：标记半径与线宽按倍数缩放，默认 1。
- **悬浮提示逐项同步显示状态**：光标悬浮格子的提示框改为与当前所有已开启的显示项**严格同步**，
  未开启的显示项不再出现在提示里：
  - 总开关 `hoverTip`（关闭后不产生任何提示，鼠标 / 触控悬浮处理一并短路）；
  - 内容开关 `hoverTipState`（环境状态）、`hoverTipAgent`（移动体）、`hoverTipTrail`（轨迹信息）、
    `hoverTipMarkers`（标记信息）；
  - 标记行同时受样式开关约束：起点 / 终点需 `showStartEnd` 开启，边界进出点需 `showCrossings` 开启
    （数据取自 `prepareCrossingCells()` 建立的逐格索引，与画面标记同源于一次 `unwrapTrail` 解算，
    两者永不脱节），移动体行需 `showBody` 开启，轨迹行需 `showTrail` 开启。
- **提示新增「上一次经过」步数**：轨迹模型逐格保留访问步数序列 `ticks`，新增 `visitStatsAt(info, tick)`
  与 `revisitReached(info, tick, min)`；悬浮提示在原有「次序 / 首次 / 末次 / 共 N 次」之外，补出
  「本次经过：第 N 步（截至当前共 M 次）」与「上一次经过：第 N 步」（本格首次经过时显示「无」），
  回溯口径与播放进度一致。
- **重访格高亮 `showRevisit`**（默认关闭）：多次经过的格子额外补一层高亮，
  参数含 `revisitMin`（重访判定次数 2 ~ 20）与 `revisitAlpha`（高亮不透明度 0.05 ~ 0.6）；
  逐帧判定结果按「播放步数 + 阈值」缓存（`_revisitCache`），不做全量重扫。
- **悬停行列准线 `hoverCrosshair`**（默认关闭）：悬浮时高亮光标所在行 / 列，
  参数 `hoverCrosshairWidth`（0.5 ~ 4）调节准线宽度。

### 优化

- 配置差异比对补齐上述新字段的中文标签（含「边界进出点」「重访格高亮」「悬停行列准线」及各内容开关）。
- 设置面板「显示内容」新增独立的「边界进出点」开关；「渲染效果」不再重复放置该项；
  新增重访格高亮、悬停行列准线、悬浮提示（总开关 + 内容子项）三组可配置控件。
- 旧的「边界穿越标记」措辞统一为「边界进出点」。

### 测试

- 新增 4 个测试分组：`轨迹访问回溯：visitStatsAt`、`悬浮提示：内容与已开启的显示状态严格同步`、
  `新增显示模块：重访格高亮与悬停行列准线（开关 + 参数）`、`边界进出点独立显示与悬浮提示同步（源码级回归）`；
  并补充「关闭轨迹后进出点仍绘制且上界随进度推进」「`crossingScale` 缩放标记半径」等独立性断言。
  当前共 **984 项断言全部通过**。

## v2.6.2

### 变更

- **「自撞即判定死亡」与「撞到自身」改为互斥自动关闭**：原先开启前者会把结束规则「撞到自身」强制锁定为只读
  （派生字段 `endConditions.selfCollisionLocked`，面板置灰不可修改），用户必须跳到另一个配置页才能改动。
  现改为**勾选即时联动**，保留「两项互斥」这一核心约束：
  - 勾选任一开关时，系统立刻取消并关闭另一项——在「结束规则」里勾选「撞到自身」会自动关闭
    「扩展机制 → 蛇死亡转化 → 自撞即判定死亡」，勾选后者也会自动关闭前者；开启「蛇死亡转化」总开关
    且死亡项为开时同样触发。联动发生时给出提示，用户无需跳转任何页面。
  - 配置规范化阶段（`normalizeConfig`）保留同一约束：若「自撞即判定死亡」已生效
    （`transform.enabled && dieOnSelfCollision`），则关闭「撞到自身」；载入模板 / 导入配置 / 载入分享链接
    在面板重建前统一收敛，保证界面显示的勾选状态与实际运行语义一致。
  - 删除派生锁定字段 `endConditions.selfCollisionLocked` 及运行时对它的判断——`simulation.js` 中
    「撞到自身」结束判定恢复为只看事件本身（转化死亡事件仍不计入），并移除对应的 `.end-row.locked` 置灰样式。
  - 结束规则分组内新增互斥说明与「一键改为「撞到自身」结束」按钮，可在原地反手切换两种语义。
  - 配置诊断 `transformOverridesSelfCollisionEnd` 文案改为「已因互斥自动关闭」，并给出
    「改由「撞到自身」终止运行」的一键方案。
- **「查看结束规则」支持非结束规则原因**：「生命耗尽」「蛇已全部转化为环境」这类不在结束规则列表里的结束原因，
  原先只提示「不属于结束规则列表项」；现按 `END_REASON_GROUP` 映射跳到真正控制它的分组
  （生命机制 / 蛇死亡转化）并高亮该分组，定位一步到位。

### 优化

- **结束规则分组新增徽标**：标题右侧显示「已启用 N 项」，分组收起时也能一眼看出当前有多少条结束规则生效。
- **「连续自撞上限」置灰判断简化**：是否失效由「撞到自身」一项唯一决定，不再重复判断「自撞即判定死亡」。

### 测试

- 重写互斥相关断言：锁定字段已从配置中移除、单向自动关闭、总开关未启用时不触发、诊断附带一键方案；
  新增源码级回归断言 10 项（联动函数、两处开关接入、重建前兜底收敛、锁定实现与样式移除、即时提示、
  一键切换、分组徽标、非结束规则原因跳转）。当前共 **903 项断言全部通过**。

## v2.6.1

### 修复

- **边界网格只显示一半轨迹 / 跨缝轨迹整段缺失**：轨迹绘制此前按「相邻两点的像素间距 > 1.7 格」断段，
  而环绕边界（`wrap`）下相邻两步的格坐标会「瞬移」到对侧，两个真实像素点相隔整张画面（数十格）→
  跨缝处被强制断段：接缝前后的轨迹整段丢失，靠近边界的网格只显示半条轨迹。
  现改为与体节渲染同一套解算（`unwrapTrail`）：① 沿轨迹累加环绕最短位移 `Grid.wrapDelta` 得到
  **连续展开坐标**（展开坐标下相邻点恒差 1 格）；② 按整圈平移量把展开坐标折回网格区域附近，
  每段记录自己的平移量，跨缝处由「滑出侧一直画到边界 + 滑入侧从对侧接着画」两段衔接（重叠一个点）；
  ③ 绘制统一裁剪到网格区域（`clipTrail`），越界的镜像段不会落到四周留白区。
  结果：边界网格的轨迹显示覆盖率达到 100%，所有穿越边界前后的轨迹段都连续完整，
  且不再出现横穿整张画面的长条。`src/core/exporters.js` 的轨迹 SVG 导出同步改用同一解算。
- **界面「自适应难度」表述有误**：该开关调节的是**播放速度**（拥挤度升高时按倍率放慢播放），
  与运行期难度读数无关，现全部更正为「自适应速度」（控制条复选框、提示文案、代码注释与 README）。

### 新增

- **轨迹颜色分级映射**（`style.trailColorMode`，设置面板「展示样式 → 轨迹」）：
  `fade` 按新旧渐隐（默认，与旧版本视觉一致）· `visit` 按经过次数（热度）取色 · `order` 按经过次序取色；
  色带复用既有 `paletteColor`，取值分别按「(经过次数-1)/最大次数」与「(经过次序-1)/格数-1」归一化，
  一次构建逐帧复用，轨迹变化时自动失效重建。
- **轨迹尖端平滑过渡**（`style.trailSmooth`，默认开启）：播放位置落在两帧之间时，
  沿展开坐标把轨迹头部补到步内位置（`Renderer.smoothHead` / `strokeSmoothTip`），
  轨迹随蛇头平滑滑动而不是逐格跳变；只在头部与下一点同属一段时补画，因此补出的小段永远不会横穿画面。
- **边界穿越标记**（`style.showCrossings`，默认开启）：在接缝两侧各画一个标记——
  滑出侧为空心环、滑入侧为实心点（`Renderer.drawTrailCrossings`），标记随播放推进依次出现。
- **边界穿越事件日志**（`events.logCrossings`，默认开启）：穿越不再只是画面特效，
  同时写入一条可回溯的规则日志（`ruleId: 'boundary'` / `ruleName: '边界穿越'` / `trigger: 'wrap'`，
  说明从何处滑出、从何处滑入），并把累计次数写入 `stats.wrapCrossings` 与摘要字段；
  关闭开关后不影响运行结果（步数 / 帧序列完全一致），只是不再产生日志与计数。

### 变更

- **配置自动存档与状态存档合并为同一分组**：侧边面板原先的「配置自动存档」「状态存档」两个独立分组
  合并为「配置 / 状态存档」，内部以「配置自动存档」「状态存档」两个小标题分区，减少侧栏分组数量。
- **排行榜「本轮成绩进入本地排行榜第 1 名」提示默认关闭**：`state.showRankToast` 初始值改为 `false`，
  开关与持久化键（`gridsneaker:rank-toast`）保留，可在「展示样式 → 界面配置」按需开启；
  榜单本身的记录与排序逻辑完全不变。

### 优化

- **性能 · 渐隐 / 颜色档样式表**（`Renderer.fadeStyleTable`）：把每一档的描边参数（颜色 / 不透明度 / 线宽）
  预先算好并按档位查表，逐段重新插值配色与拼接颜色字符串（每帧描边的主要开销）被消除；
  颜色分级启用时样式表扩展为「亮度档 × 颜色档」的组合档，同样走查表而不增加逐帧开销。
  同进程 A/B 基准（`tools/bench-trail.mjs`，90×60 环绕网格 2400 步）由基线约 2.9~3.5 万帧/秒提升到约
  **7.2 万帧/秒**（约 2.1 倍，远超 +15% 的目标）；边界解算同样快于基线。

### 测试

- `tests/core.test.mjs` 新增 4 组共 81 项断言：
  - 「边界网格轨迹覆盖率（100%）与跨缝段连续性」：段内相邻点恒差 1 步、段数 = 穿越次数 + 1、
    接缝点被两侧同时覆盖且两段位置恰相差整圈、全部轨迹点都能折回网格区域内、**边界网格轨迹覆盖率 100%**、
    段内像素间距不超过一格，以及真实 `draw()` 下绘制点数覆盖全部轨迹点且无横穿线段。
  - 「轨迹颜色分级映射」：枚举与白名单、分享链接往返、热度/次序取值公式与色带差异、缓存复用与失效。
  - 「轨迹尖端平滑过渡」：默认值与容错、所有相邻步（含跨缝）都能补出尖端且长度不超过一格、
    步内进度 0.5 时尖端恰为半格、关闭后不再补画。
  - 「边界穿越标记与事件日志」：开关与容错、日志字段与统计口径、关闭后运行结果不变、
    标记分处接缝两侧且落在网格区域内（开启 / 关闭的圆点绘制次数差 = 2 × 穿越数）。
- 另新增源码级回归断言：排行榜第 1 名提示默认关闭且保留开关、存档分组已合并、
  界面不再残留「自适应难度」措辞。当前共 **893 项断言全部通过**。

## v2.6.0

### 新增

- **生命机制（多生命系统）**：移动体新增 `life` 配置组，把「一次致命即结束」改为「扣命 → 原地重生 → 生命耗尽才最终死亡」：
  - **初始生命可自定义**：设置面板提供「初始生命」滑杆（`initialLives`，夹取到 `[LIFE_MIN, LIFE_MAX] = [1, 9]`），越界数值会被规范化收敛并在配置诊断中给出提示（`lifeInitialClamped`）。
  - **环境动态增减生命**：`life.items.gainStates` 指定的格子（默认「交互标记物」）踏入即 +`gainAmount` 条命（可选 `consumeGain` 拾取后清空该格）；`items.lossStates` 指定的格子作为非致命陷阱，踏入即 -`lossAmount` 条命。生命上限受 `LIFE_MAX` 保护。
  - **单条生命耗尽即原地重生**：`consumeLifeOnDeath` → `respawnAgent` 保留蛇头位置、朝向与得分 / 步数等关键进度，仅把蛇身长度重置为 `life.respawn.length`，并给出 `invincibleTicks` 步无敌窗口与 `lifeRespawn` / `respawn` 高亮（渲染层播放重生动画）；重生后自动改选一个「不撞墙、不撞自身」的安全方向，避免下一帧原地反复丢命。
  - **生命耗尽才最终死亡**：生命归零时经统一入口 `markLifeDepleted` 计入 `finalDeaths`、写入 `lifeDepleted` 事件与高亮，再按原有「蛇死亡转化」或结束规则收尾。
  - **低生命预警**：`life.warnThreshold` 设定预警阈值，剩余生命降至阈值时写入一次 `lifeWarning` 高亮与事件；界面在播放到该帧时弹出提示（`notifyLifeEvents`）。
- **死亡即停（默认关闭「死亡后仍可移动」）**：`life.keepMovingAfterDeath` 默认 `false`——蛇触发死亡判定后立即停止所有移动逻辑，死亡帧起 `captureFrame` 不再输出蛇头与蛇身（`segments: []` / `length: 0`）。仍保留开启后的旧表现（僵尸态 `agent.zombie`，保留蛇头蛇身继续推进）。
- **自撞判定与自撞结束规则互斥绑定**：新增派生锁定字段 `endConditions.selfCollisionLocked`——「自撞即判定死亡」（`transform.dieOnSelfCollision`）开启时，「撞到自身」结束规则自动禁用且面板置灰不可手动修改；关闭后恢复可编辑。该字段为派生值，不写入默认配置、也不改写「撞到自身」的原始值，因此关闭后原样恢复。对应诊断项 `transformOverridesSelfCollisionEnd`。
- **设置面板四大类选项卡**：按「核心规则 / 视觉显示 / 场景与运行 / 扩展机制」（`CONFIG_TABS`）归类合并，删除冗余的独立选项卡；原分组整体迁移、不做拆分，保证所有功能可访问性不变，并支持从「查看结束规则」等入口直接跳到目标分类。
- **配置自动存档**：`AUTO_SAVE_KEY = 'gridsneaker:autosave'`，播放 / 跳帧时按 1.2 秒防抖写入「配置 + 播放位置 + 统计口径」；刷新后配置由本地自动恢复，若存档配置与当前配置一致则自动跳回上次进度。
- **得分排行榜**：每轮运行结束自动把成绩写入本地（按总分降序、限额保留、去重指纹避免重算重复入榜），归入「统计模块」内，支持查看与一键清空。
- **移动端触控优化**：画布不拦截 `touchstart` / `touchmove` 默认行为，保留滚动与双指缩放（由 CSS `touch-action` 约束），并放大触控目标。
- **统计与事件**：新增 `lifeLosses` / `lifeGains` / `respawns` / `lifeWarnings` / `finalDeaths` / `maxLives` 运行期统计与摘要字段；帧事件新增 `lifeGain` / `lifeLoss` / `trapHit` / `lifeRespawn` / `lifeWarning` / `lifeDepleted` 标签与对应特效（`lifeGain` / `lifeLoss` / `lifeWarning` / `lifeDepleted` / `respawn`）。

### 修复

- **扣命重生被结束规则提前终止**：生命机制启用时，扣命重生所在帧已写入的 `selfCollision` / `wall` / `obstacle` 事件仍会命中 `checkEndConditions` 的对应结束规则，导致整轮运行在第一处致命点就结束（只扣 1 条命）。现新增 `absorbFatalEvent`，把「已被生命机制吸收」的致命事件标记为 `absorbed`，`checkEndConditions` 的 `wall` / `outOfBounds` / `obstacle` / `selfCollision` 判定跳过该标记，使生命耗尽才是唯一的最终死亡。
- **陷阱耗尽生命未计入最终死亡**：陷阱扣光最后一条命时走的是 `stepAgent` 的直通分支，此前只累加 `lifeLosses` 就直接 `agentEnd`，缺少 `finalDeaths` 统计与 `lifeDepleted` 事件 / 高亮。现统一改由 `markLifeDepleted` 收尾，五条致命路径（自撞 / 撞墙 / 越界 / 障碍 / 陷阱）统计口径一致。

### 变更

- **自撞结束规则语义收敛**：生命机制启用且仍有剩余生命（含无敌窗口）时，自撞只作碰撞记录，不再触发「撞到自身」结束规则。
- **设置面板选项卡重命名（消除误导性命名）**：原「操作控制」「难度参数」两个选项卡名与面板实际内容不符——
  面板中并不存在「操作控制」类选项；而真正的「难度」是运行期的拥挤度难度评估（`src/core/difficulty.js`，
  读数位于控制条与统计面板），与这两个选项卡的内容无关。现按实际内容重命名，并同步内部 key 以保持命名一致：

  | 原命名 | 新命名 | 内部 key | 面板实际内容 |
  | --- | --- | --- | --- |
  | 操作控制 | 场景与运行 | `control` → `scene` | 场景名称与描述、随机种子（可复现）、规则执行方式 |
  | 难度参数 | 扩展机制 | `difficulty` → `extend` | 多蛇与交互、蛇死亡转化、生命机制、元胞自动机 |

  涉及文件：`src/ui/app.js`（`CONFIG_TABS` 的 label / key、选项卡面板归属、注释）、`styles.css`（选项卡注释）、
  `CHANGELOG.md`（本记录）、`GridSneaker.html`（构建产物重新生成）。分组与控件未做任何迁移或删改，功能可访问性不变。
- **去掉「游戏」自称（产品定位为「网格移动动画模拟平台」）**：程序还称不上游戏，凡把本产品称为「游戏」的表述一律删改；
  经典 CA 规则「生命游戏（Conway's Game of Life）」为专有名词，予以保留。

  | 原命名 | 新命名 | 位置 |
  | --- | --- | --- |
  | 游戏核心规则（选项卡） | 核心规则 | `CONFIG_TABS` |
  | 游戏进度自动存档（分组） | 配置自动存档 | 侧边面板 |
  | 游戏状态存档（分组） | 状态存档 | 侧边面板 |
  | 保存当前游戏状态 / `saveGameState` / `loadGameState` | 保存当前状态 / `saveState` / `loadState` | `src/ui/app.js` |
  | 「默认游戏模式」 | 「默认配置」 | `src/core/presets.js` 预设描述 |
  | 「默认游戏模式：边界行为…」 | 「默认配置：边界行为…」 | `tests/core.test.mjs` 测试分组名 |

  涉及文件：`src/ui/app.js`、`src/core/presets.js`、`styles.css`（注释）、`tests/core.test.mjs`、`GridSneaker.html`（构建产物重新生成）。
- **得分排行榜归入统计模块**：「本地得分排行榜」原为侧边面板的独立分组，现并入「统计模块」内部
  （与「坐标筛选查询」同级），成绩属于统计结论的留档，与统计口径同处一处更易查找；`renderScoreboardList` 的局部刷新机制不变。

### 测试

- `tests/core.test.mjs` 新增「生命机制：多生命 / 扣命重生 / 生命耗尽 / 死亡即停 / 自撞互斥」测试块：
  配置规范化与夹取、配置诊断、自撞互斥绑定的正向 / 反向 / 行为验证、扣命重生（保留蛇头与进度 + 重生动画事件）、死亡即停与僵尸态对照、
  环境增减生命（增益 / 上限 / 陷阱 / 关闭对照）、未启用生命机制时的旧行为回归，以及「致命事件吸收」（撞墙扣命重生后不误终止整轮运行）。
  当前共 **812 项断言全部通过**。

## v2.5.0

### 新增

- **蛇死亡转化（自撞即死亡 → 并入元胞自动机）**：移动体规则下新增「蛇死亡转化」配置组
  （`transform`），把「自撞」从「结束运行」重新定义为「蛇死亡」：
  - **自碰撞死亡判定**：`dieOnSelfCollision` 开启后，蛇头撞到自身任意身体节点即判定该移动体死亡
    （`Simulation.transformAgent`），主移动体也不例外；但**刻意不结束整轮运行**，
    主循环、环境规则与元胞自动机继续推进，符合「保持程序进程正常运行」的语义。
    关闭该开关时完全沿用原有「自撞处理」策略与结束规则，旧场景零变化。
  - **两个可调节概率参数**：`globalProbability`（全局触发概率，蛇死亡后是否启动转化流程）与
    `segmentProbability`（分段转化概率，每一段身体节点独立转化为环境状态的概率），
    两者都在配置面板中实时可调（滑杆 + 数字输入，步长 0.01），并随时间/数字双向同步。
  - **转化逻辑无缝衔接**：命中概率的身体节点通过 `World.set` 写入 `transform.state` 指定的环境状态，
    同时置 `cellsDirty`，使帧缓存、元胞自动机与画面共用同一份 `world.cells` 数据源，
    保证 CA 逻辑一致性与视觉连贯性。
- **转化视觉过渡动画**：新增 `transform` 高亮类型与 `Renderer.drawTransformEffect`
  （体节色块向目标状态色淡入 + 收束圆环，方格 / 六边形双分支），
  在既有 `EFFECT_LOOKBACK` 窗口内连续过渡；单次转化的过渡高亮条数受
  `MAX_TRANSFORM_HIGHLIGHTS`（4000）上限保护，避免超长蛇逐帧携带超大数组。
- **碰撞预警提示**：`safety.warnSelfCollision` 开启后，每步调用 `Simulation.collectCollisionWarnings`，
  把「下一步会撞到自身身体」的邻格标为警示色（`warning` 高亮 + 统计 `collisionWarnings`）；
  默认关闭，避免长蛇场景下的额外逐格开销。
- **游戏状态存档与读取**：侧边面板新增「游戏状态存档」分组（`SAVE_KEY = 'gridsneaker:saves'`，
  上限 20 份），可保存当前配置 + 保存帧号 + 统计口径，一键「读取」跳回保存时刻，
  支持逐条删除与清空；浏览器存储配额不足时自动剥离皮肤图片降级重存。
- **概率参数试算**：转化配置组内置蒙特卡洛试算（`runSample`，2000 次采样）与期望节点数实时估算，
  便于在调整概率时直观看到预期转化规模。
- **统计与事件**：新增运行期统计 `transformDeaths`（自撞死亡）、`transformedCells`（转化节点）、
  `collisionWarnings`（碰撞预警）与摘要字段，统计面板同步展示；帧事件新增
  `transform` / `transformSkipped` / `transformDeath` 标签。

### 变更

- **自撞不再污染自撞结束规则**：转化模式下自撞产生的事件带 `transformed` 标志位，
  `checkEndConditions` 的「撞到自身」与自撞计数类结束规则会跳过该事件；
  转化死亡也不再累加 `selfCollisions`，因此它只由「蛇死亡」流程接管，不会被自撞规则收尾。
- **新增结束原因**：`transformDone`（蛇已全部转化为环境）——已发生自撞死亡、场上不再有存活移动体、
  且没有元胞自动机需要继续演化时收尾，避免无意义的空转；
  启用 CA 时则保持运行，让并入环境的节点继续演化。
- **主移动体消失后的主体选择**：`stepOnce` 选取规则主体时由「只认主移动体」改为
  「主移动体优先 → 退回到仍存活的移动体 → 首个移动体」，保证主移动体转化消失后规则与结束条件仍有可用对象。

### 修复

- **死亡 / 排斥特效永不触发**：`killAgent` 与多蛇「排斥」回退分支此前只写 `tickEvents` 未写
  `ctx.highlights`，而渲染层以 `highlights` 驱动特效，导致 `agentDeath` / `repel` 两个特效分支始终空转；
  现补写对应高亮，特效按预期播放。

### 测试

- `tests/core.test.mjs` 新增「自碰撞死亡与转化为元胞自动机」共 37 项断言：
  概率参数夹取与非法目标状态回退、自撞死亡不终止运行、两个概率为 0 的边界行为、
  关闭开关 / 未启用转化时回归旧行为、启用 CA 后不提前收尾、碰撞预警高亮与统计、
  配置诊断（`transformNoEffect` / `transformOverridesSelfCollisionEnd`）。
  当前共 **711 项断言全部通过**。

## v2.4.0

### 新增

- **自定义皮肤（上传图片）**：「移动体与身体」下新增「自定义皮肤（上传图片）」分组，
  可分别为**蛇头**与**蛇身**选择本地的 JPG / PNG / WebP 图片（`SKIN_MIME_TYPES`），
  上传后立即渲染到画布，点「清除」恢复纯色 / 渐变配色。
  校验按「格式 → 体积（≤ 2 MB）→ 可解码 → 尺寸（每条边 16~2048 像素）」顺序执行，
  任一环节失败都保持原皮肤不变并提示具体原因（`readSkinFile`）。
  渲染方案：蛇身走「离屏层描轮廓 → `source-in` 图案填充 → 整层贴回」的蒙版合成
  （`Renderer.drawSkinRibbon` / `skinPattern` / `ensureSkinLayer`），与纯色绘制共用同一套
  `ribbonRuns` 几何，因此宽度收束、跨越边界的镜像副本都与原渲染完全一致；
  蛇头用 `clip()` 裁切后贴图（`drawSkinHead`），保持按朝向绘制眼睛的既有行为。
  图片以 dataURL 存入 `body.skin.head` / `body.skin.body`：随「导出 JSON」保存、写入浏览器本地存储
  （超配额时自动剥离皮肤重试），但**不写入分享链接**（`stripSkinAssets`，图片体积远超链接长度上限）。
  新增 `isSkinImage` / `normSkin`：配置规范化只接受受支持的图片 dataURL，非法值一律回退为「未设置」。
- **单文件打包的静态资源压缩**：`tools/build-single-file.mjs` 新增轻量压缩
  （`scanJs` / `minifyJs` / `minifyCss`），对字符串 / 模板 / 正则字面量无感的区域去掉注释与缩进，
  仅在与词法相邻处按需补一个空格（词与词之间、`++` `--` `//` 等运算符之间），并保留换行以保证 ASI 语义不变；
  正则在「上一个记号是 `return` / `typeof` / `case` 等关键字」时按正则处理，避免把除号误判为正则。
  产物由 446.1 KB 降至 327.8 KB（约 −26%）；`--no-minify` 可输出未压缩的 `GridSneaker.raw.html` 用于排查。

### 变更

- **设置面板默认全部收起**：`group()` 的默认展开条件由「未显式关闭即展开」改为「仅显式 `open: true` 才展开」，
  侧边面板与配置面板的全部设置分组初始状态只露出分组标题，界面更清爽；
  原先默认展开的「预设模板」「统计模块」「规则触发日志」「配置诊断」同步改为收起。
  用户的手动展开 / 收起仍由 `groupOpenState` 记忆，面板重建后保持原状。
- **边界行为默认值改为「穿越到另一侧」**：`defaultGrid().boundary` 由 `stop` 改为 `wrap`，
  蛇头触碰画布边界时从对侧对应位置重新出现；「随机游走」预设同步改为 `wrap` 并关闭「撞墙结束」，
  使默认游戏模式即为穿越模式。

### 修复

- **边界反弹贴边掉头被误判为自撞死亡（安全避撞）**：`boundary === 'bounce'` 时，
  反向落点若正好压在自己的身体上（贴边掉头撞到「脖子」）或落在界外（贴角），
  原先会直接触发自撞 / 撞墙结束。现改为调用新增的 `Simulation.bounceSafeOption()`，
  在其余方向中随机择优一个「界内 + 未被阻塞 + 不撞自身」的落点；无可行方向时才回落到原始反弹逻辑。
  该分支只在反弹模式下生效，穿越等其它边界策略的逻辑与结果完全不变。

### 测试

- `tests/core.test.mjs` 新增 35 项断言：「默认游戏模式：边界行为为「穿越到另一侧」」、
  「边界反弹的安全避撞（贴墙掉头不自撞）」（含「反弹不撞自身」与「穿越模式行为不受影响」的对照）、
  「自定义皮肤：格式校验 / 规范化 / 分享链接剥离」，并把「边界行为枚举取中文文案」的断言改为
  在穿越（新默认值）基础上切到停止。当前共 674 项断言全部通过。
- 补全无头渲染器桩对象的皮肤字段（`skinSrc` / `skinImg` / `onSkinLoad` / `skinLayer`），
  使不加载图片的测试环境自动走纯色渲染分支。

### 文档

- 新增 `README.md`：功能特性、快速开始（单文件 / 本地 HTTP）、控制条与快捷键、
  配置面板分组说明、边界行为与自定义皮肤的使用与校验规则、目录结构、
  构建与测试命令、GitHub Pages 等静态托管部署指引、浏览器兼容性说明。

## v2.3.0

### 新增

- **模板载入的改动检测**：新增 `src/core/config-diff.js`（纯函数差异比对），
  按「模板基准 ↔ 当前配置」逐字段比对，输出中文化的差异条目（枚举取值翻译为界面用语，
  规则表 / 状态表等数组按整体内容变化报告一条，不做无意义递归展开；
  版本号与平滑派生字段不参与比对）。
  「载入模板」前若检测到用户自定义改动，弹出确认窗口分节列出将被覆盖的改动项（如 `宽：24 → 40`），
  提供**「覆盖保存」**与**「取消载入」**两个操作；无改动时直接载入，不打扰用户。
- **模态对话框基础设施**：`src/ui/forms.js` 新增 `openDialog` / `confirmDialog` / `alertDialog` / `dialogOpen`，
  支持分节明细、脚注、Esc 与遮罩点击关闭、焦点归还；破坏性操作（`danger`）默认聚焦「取消」，
  避免误按回车覆盖配置。对话框打开时全局快捷键自动让位。
- **配置撤销栈**：载入模板、导入配置、载入链接配置、恢复上次配置、应用 CA 快捷模板前记录现场，
  可按 `Ctrl/Cmd + Z` 或点击侧边面板「撤销上次变更」回退（保留最近 12 次）。
- **配置面板设置搜索**：面板顶部新增搜索框，按关键词过滤设置分组并自动展开命中的分组，
  实时提示匹配分组数量，一键清除并还原原有折叠状态。
- **得分明细弹窗**：点击统计面板的「得分」格可查看总分、相对分与 6 个分项的构成说明，
  并附当前地图的最高分记录，让得分结果可解释。

### 变更

- **「得分 / 评分等级 / 难度 / 拥挤度」默认隐藏**：这四类读数同属「运行评价」，
  原先始终显示，现改为默认隐藏，可在「展示样式 → 界面配置」勾选「得分 / 评级 / 难度 / 拥挤度」开启，
  选择结果持久化到 localStorage（`gridsneaker:show-score`）。
  显隐为整体开关：控制条整行（得分行 + 难度行）与统计面板的「得分」「评分等级」两格同步切换，
  隐藏时不留下空行占位；地图最高分记录不受影响，始终照常统计与保存。

### 修复

- **模板载入出现「幻影改动」（`（空） → NaN`）**：`defaultConfig()` 的长度策略缺失
  `growth.minLength` 与 `shrink.maxLength`，`normalizeConfig` 用未定义值兜底时
  `Math.round(undefined)` → `NaN`；而基准配置经 JSON 往返后 `NaN` 会变成 `null`，
  两者比对即判定为「用户改动」，导致什么都没改也会弹出覆盖确认框。
  修复：`defaultConfig()` 补全这两个默认值（1 / 100000）；
  同时 `diffConfigs` 对不可被 JSON 表示的空值（`NaN` / `Infinity` / `undefined` / `null`）做归一化，
  `describeConfigValue(NaN)` 显示为「（空）」而非「NaN」，从结构上消除此类假差异。
- **「网格与坐标」格数提示不刷新**：改「宽 / 高」不会重建配置面板，
  底部的「网格共 N 格」是渲染期静态文本，一直停留在旧值。改为随宽高即时原地刷新。

### 测试

- `tests/core.test.mjs` 新增「配置差异比对（模板载入改动检测）」共 46 项断言，覆盖：
  相同配置零差异、标量 / 枚举 / 布尔差异文案、数组整体变化只报一条、派生字段不参与比对、
  值描述（空值 / 数组 / 布尔 / 超长截断）、分组聚合与顺序、空值容错，
  以及上述 NaN 幻影改动与长度策略有限性的回归断言。当前共 639 项断言全部通过。

## v2.2.0

### 修复

- **边界穿越的体节视觉分离（低速尤其明显）**：跨缝时原先只为「本步自身穿越的连续体节」生成对侧镜像，
  当链中段 / 尾段单独穿越而相邻体节未穿越时，该体节的主副本会被插值到越界一侧、与其余体节相距约整张画面，
  且镜像 run 只有 1 节 → 只画出一个孤立圆点，表现为「蛇身数节断开」。
  `Renderer.agentPoints` 改为三步解算：① 以蛇头为锚沿链累加 `wrapDelta` 得到**连续展开坐标**
  （展开坐标下相邻两节永远只差一格）；② 按「展开坐标 + 本步环绕最短位移」插值，单步位移始终不超过一格；
  ③ 整条链再按网格周期平移出镜像副本（新增 `Renderer.periodicRuns`），副本与本体形状完全一致。
  任何插值进度下本体链与镜像链都保持连续，不再出现孤立体节。
  新增断言：本体链相邻体节间距 ≤ 1.6 格、镜像链长度与本体一致且同样连续、跨缝场景确实被覆盖。
- **蛇头位于画布四角时误判「无路可走」**：结束条件 `noMove` 遍历方向时对越界邻居直接 `continue`，
  未考虑边界穿越，四角蛇头的两个越界方向被跳过 → 剩余方向被占即误报无路可走。
  新增 `Simulation.resolveCandidate()`：`boundary === 'wrap'` 时把越界候选 `grid.wrap()` 回网格内再参与判定，
  其余边界策略保持「不可达」语义不变。`noMove`、`selfBlocks`、`filterSafeOptions`，
  以及 `stepAgent` 的 `randomTurn` / `custom` 边界分支、障碍物 `turn` 策略、自撞后的 `forcedTurns` / `randomTurn` 候选
  统一改用该方法，四角 + 穿越时可正确算出可移动方向。
  新增断言：四角 + 穿越不判 noMove、四角被完全围住时仍正确判定 noMove、非穿越边界下判定行为不变。

### 新增

- **得分系统**：新增 `src/core/score.js`，把一轮运行的统计量折算为「总分 + 等级 + 6 个透明分项」
  （成长 / 探索 / 存活 / 交互 / 收尾奖励 / 碰撞罚分），总分下限为 0；
  等级按「总分 ÷ 网格格数」的相对分判定（S / A / B / C / D），不同地图尺寸可横向比较。
  `Simulation.run()` 的 `summary.score` 直接携带结算结果；统计面板新增「得分」「评分等级」两项，
  控制条新增得分行（`得分 X（等级） · 最高分 Y`），**最高分按地图指纹（类型 / 尺寸 / 边界策略）持久化到 localStorage**，
  刷新记录时给出提示。
- **动态难度（自适应播放速度）**：新增 `src/core/difficulty.js`，按「已探索覆盖率 + 蛇身长度占比 + 存活移动体数」
  加权得到拥挤度（0~1，四舍五入消除浮点噪声），据此划分 5 档难度（轻松 / 正常 / 紧张 / 困难 / 绝境）
  并给出建议播放倍率（1 → 0.85 → 0.65 → 0.45 → 0.3）。
  控制条新增「自适应难度」开关（持久化），开启后播放时按当前帧的难度倍率自动放慢，
  拥挤度与难度等级实时显示在控制条。
- **边界穿越穿梭特效**：模拟层在 `boundary === 'wrap'` 发生穿越时写入 `{ type: 'wrap' }` 画面高亮，
  记录滑出侧坐标（`fromCol` / `fromRow`，位于网格外）与落点（滑入侧）；
  `captureFrame` 的高亮投影同步保留这两个字段。
  `Renderer.drawEffects` 新增 `drawWrapStreak`：从落点朝进入方向绘制由粗到细、由亮到暗的拖尾光带，
  并裁剪到网格区域，让「从对侧滑入」在画面上有明确方向感。
- **操作手感优化**：新增速度档位按钮（0.5 / 2 / 8 / 30 / 120 步/秒，新增 `setSpeed()` 统一收敛与回填滑块）；
  键盘新增 `Shift + ← / →` 跳转 10 帧、`-` / `=` 播放速度减半 / 加倍、`R` 回到首帧、`Esc` 暂停，
  快捷键提示同步更新。

## v2.1.0

### 修复

- **边界穿越（环绕边界）导致蛇身缺段**：`createInitialAgent` / `spawnSnake` 在铺设初始身体时遇到越界就 `break`，
  起点靠近地图边界时初始身体被截断，表现为「穿越后身体片段分离 / 长度与设定不符」。
  现改为按 `grid.wrap()` 环绕延伸（节数上限取整张地图格数，避免体节重叠），
  同时 `fittedBodyLength` 同步环绕语义，配置诊断不再误报「身体被边界截断」。
  新增四方向（上 / 下 / 左 / 右）全程断言：初始长度、体节环绕最短位移相邻、每步落点 = 环绕后的格点、
  每帧体节链连续、确实发生过穿越。

### 变更

- **多层选项卡差异化配色**：分组（`<details class="group">`）按嵌套深度写入 `--grp-h/s/l` 变量，
  色相按层级梯度递增（父级偏蓝、子级依次偏移），饱和度与亮度逐级提升，配合左侧层级色条与标题配色，
  父级 / 子级一眼可辨，整体仍沿用同一套暗色主题。
- **增长概率默认值改为 1**：`body.lengthPolicy.growth.probability` 默认由 0.1 调整为 1（吃到 / 达成条件后必定增长），
  配置面板滑块直接读配置，默认显示同步为 1。
- **新增「生命游戏（交互版）」预设模板**：生命游戏生成的活动方块（`alive`）与蛇头重合时被吞噬（格子回到 `empty`）、
  蛇长度 +1；原无交互模板重命名为「生命游戏（无干涉版）」。
- **统计模块集成坐标筛选查询**：「坐标筛选查询」由独立分组改为内嵌在「统计模块」选项卡内的子选项卡，
  结果与统计口径（实时 / 总计）共用同一轨迹数据源，实时同步。
- **界面术语中文化**：新增 `STATE_LABELS` / `stateLabel()`（`world.js`）与 `DIR_LABEL_CN` / `dirLabel()`（`grid.js`），
  状态下拉、状态列表编辑器、交互标记物状态、CA 规则状态、画布格点信息面板全部显示规范中文
  （同时通过 `title` / 括号保留内部键名，便于与配置、分享链接对照）；`OBJECT_LABELS` 补齐 CA 状态中文名；
  方向标签统一复用 `DIR_LABEL_CN`，删除重复常量。

### 优化

- **性能 · 网格线离屏缓存**：网格几何与配色只在网格 / 尺寸 / 样式变化时改变，改为缓存到离屏画布后每帧一次 `drawImage`，
  消除大网格下逐格 `beginPath + stroke`（200×200 网格原来每帧约 4 万次描边）。
- **性能 · 坐标筛选结果记忆化**：以「轨迹指纹 + 查询条件」为键缓存筛选结果与序数范围统计，
  避免「执行筛选 → 同步滑块 → 渲染列表」流程和播放中重复全量扫描轨迹点。
- **性能 · 转向分布原地更新**：行结构固定，改为创建一次后原地更新宽度与文本，播放时不再逐帧销毁重建 DOM 节点。
- **兼容性 · 分享链接在本地单文件下可用**：`file://` 打开时 `location.origin` 为字面量 `"null"`，
  原实现会生成 `null/路径#c=…` 的不可用链接；现退回到去掉片段后的完整地址，链接仍可在本机重新打开。
- **清理**：移除无引用的导出 `HARD_FRAME_CAP`、无引用的 `JOIN_MODES` 引入、`Renderer` 中只赋值不读取的暂存字段；
  筛选组合方式的文案改由 `TRAIL_QUERY_LOGIC_LABELS` 统一提供。
- **快捷模板与预设名称同步**：应用「元胞自动机模式 → 快捷模板」后同步场景名到对应预设，
  避免「预设模板」下拉仍显示上一个模板（如「随机游走」）而造成误解。

### 新增

- **统计面板新增「当前帧 / 总帧数」与「运行耗时」**：`Simulation.run()` 记录本轮实际耗时（`elapsedMs`，
  优先 `performance.now`，不可用时退回 `Date.now`），统计摘要随之可见。
- **「复制统计摘要」按钮**：一键复制当前口径的全部指标（含坐标筛选结论），便于留档与对比。
