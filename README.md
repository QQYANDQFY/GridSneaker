# GridSneaker · 网格移动动画模拟平台

一个纯前端、零依赖的网格移动动画模拟平台：在方格 / 六边形网格上驱动「蛇形移动体」前进，
叠加环境规则、元胞自动机与多蛇交互，并把每一步的轨迹与统计可视化出来。
全部逻辑用浏览器原生 ES Module 实现，无需构建即可运行，也可打包成单文件离线使用。

## 功能特性

### 网格与移动体

- **两种网格**：方格与六边形，宽高、格子像素、间距均可调；
  元胞自动机与感知条件支持摩尔 / 冯·诺依曼 / 六边形邻域与可调半径。
- **四种边界行为**：`穿越到另一侧`（默认）· `反弹` · `停止` · `随机转向`，
  以及由用户定义的自定义边界策略；穿越模式在画面四角同样可正确算出可走方向。
- **可配置起点与初始身体**：起点坐标 / 朝向、初始长度、头粗尾细的体节渲染、身体连接方式（直线 / 角度切角 / 贝塞尔曲线）。
- **转向权重**：左 / 直 / 右 / **停止** 四项的加权随机（「停止」默认权重为 0，被选中时不产生任何有效位移，
  判定链与「尝试移动到障碍物」完全一致），另有条件概率规则（按状态、朝向、步数等条件覆盖权重）。
- **长度策略**：增长 / 缩减各自独立的触发条件（步数、吃到标记物、达成条件）、数量与概率。
- **安全避撞与碰撞处理**：可选择规避自身身体 / 障碍物 / 其它移动体；自撞后可结束、掉头、随机转向或直接穿行；
  并可选**避撞触发后的处理方式**——`自动切换其他可行方向继续运动`（默认，与旧版一致）或
  `立即停止运动`（避撞介入时本步原地停止，计入「停止次数」并按「撞到障碍物」规则收尾）。

### 环境与规则

- **环境感知—条件—后果规则**：`感知范围 + 条件 + 后果` 三段式，后果支持转向、增长、缩减、变色、生成 / 销毁标记物、写入状态等，
  按优先级依次匹配，可在面板内启用 / 停用与排序。
- **元胞自动机（CA）**：可独立运行或与移动体混合运行，内置多种经典规则（生命游戏等）、
  规则表编辑、状态配色与「快捷模板」一键套用。设置面板按配置时的思考顺序收敛为
  **核心规则 / 演化规则 / 初始环境 / 运行与性能 / 交互与模板** 五个子选项卡
  （启用开关固定在子选项卡之上，任何子页都可见），面板不再是一条需要反复滚动的长列表；
  搜索时全部子页自动展开，命中项不会藏在未激活的子页里。
- **可复用的 CA 配置资产**：规则表支持**规则集导入 / 导出**（复制或下载为 JSON，
  只含规则表本身，可跨场景、跨用户传递，导入时对状态名不匹配给出中文警告）；
  初始环境支持**环境模板**保存与复用（＝状态集合 + 初始状态 + 手绘格子，
  不含移动体 / 规则 / 外观）；演化参数支持**参数预设**
  （邻域 / 半径 / 边界 / 更新顺序 / 同步方式 / 演化间隔 / 稳定判定 / 初始状态），
  可在同一套环境上快速对比不同参数组合。三处记录都保存在浏览器本地，可随时载入或删除。
- **交互反馈**：融合 / 排斥 / 生成 / 标记物反馈等交互事件在画面上以脉冲特效呈现。
- **画布格子编辑器**（默认关闭）：开启后，画布上的鼠标左键单击由「跳到首次经过的步数」切换为
  **直接增删格子元素**——单击添加、再次单击删除；点击坐标经
  `Renderer.hitTest` 反查行列号，与绘制几何完全一致（无交互偏移）。编辑工具含
  **标记物 / 障碍物 / 元胞自动机状态 / 擦除**四种：其中「元胞自动机状态」可显式指定任意非空环境状态
  （如生命游戏的「存活」），选「自动」时取状态集合中的首个非空状态，面板实时回显本次点击将放置的状态。
  放置时机可选**「初始环境（写入第 0 步）」**或**「当前步放置」**：前者是环境演化的起点，
  后者把格子写入当前所在步数、只影响该步及其之后的帧，**不回溯改写已完成的模拟帧**
  （重算后自动跳回放置所在步，便于连续编辑；每次放置都会写入运行日志，可按日志回溯）。
  同一开关下还提供**默认开启的拖拽连画**（按住左键拖动即连续绘制，起手格已放置则为连擦）、
  右键擦除、画笔尺寸（1~9 格）、按密度一键随机散布、撤销 / 重做（上限可调；快照同时覆盖
  初始环境与延迟放置，撤销不会丢补丁）、清空全部手绘 / 清除本步放置 / 清空全部延迟放置
  （破坏性操作先经确认）与已绘制格子计数。
  编辑模式开启时画布光标切换为十字准星。绘制的格子随配置保存与分享，并在模拟开始时写入世界，
  等价于「自定义初始环境」。若已启用编辑器但第 0 步既没有手绘格、元胞自动机的初始状态
  也等价于「全空」，面板会给出醒目警告「第 0 步还没有初始环境」并提供「去配置初始环境」一键跳转，
  避免运行后才发觉画面上没有任何环境单元、观测不到模拟变化。
- **自定义标记物类型**：为交互标记物新增「类型层」——同一状态可挂多个类型，各自带
  **外观样式 / 生效条件（始终 · 概率 · 长度上下限 · 步数门槛）/ 触发权重 / 内置反馈**，
  运行时先按条件过滤、再按权重抽取一个生效类型。列表默认为空，行为与旧版完全一致；
  后续新增标记物只需追加一条记录，无需改动模拟主循环。
- **障碍物陷阱**：为自定义障碍物类型附加陷阱属性，可分别设置**触发概率**与**死亡概率**
  两级可调参数（1 = 必定、0 = 从不）。蛇尝试移动到陷阱格时先判定是否触发、再判定是否致命，
  未致命则退回普通障碍物阻塞；每次判定都会写入运行日志（可关闭），便于调试。
- **障碍物交互式放置**：支持「先选自定义障碍物类型再点击格子精准放置」，以及
  「开启随机放置模式后按设定概率从随机池中抽取类型」两种模式，两种模式均支持再次点击删除。

### 多蛇与统计

- **多蛇生成与交互**：批量生成多个移动体，相遇时按设置执行碰撞、融合、排斥或穿行。
- **实时 / 总计双口径统计**：步数、长度、覆盖率、转向分布、死亡 / 结束原因等，
  坐标筛选查询内嵌于统计模块，与统计共用同一轨迹数据源并实时同步。
- **统计项显示配置**：生成新蛇、移动体消失、融合 / 排斥次数、生命机制等**低频统计项默认隐藏**，
  只保留步数、长度、覆盖率、碰撞等高频核心读数，统计面板不再拥挤；隐藏项可逐项勾选开启，
  并提供「仅核心指标 / 全部显示 / 全部隐藏 / 恢复默认」整组预设，当前显示条数实时提示。
  隐藏只影响统计格与摘要行的生成，统计与导出数据不受影响。
- **得分与难度**：把一轮运行折算为「总分 + 等级 + 6 个透明分项」，最高分按地图指纹持久化；
  拥挤度驱动的动态难度可在拥挤时自动放慢播放。
- **轨迹工具**：轨迹渐隐（线性 / 指数）、轨迹颜色分级（按新旧渐隐 / 按经过次数热度 / 按经过次序）、
  轨迹尖端平滑过渡、**轨迹方向箭头**（沿已播放轨迹按间隔标注前进方向，间隔 / 尺寸 / 颜色可调）、
  **热点轨迹过滤**（只绘制经过次数达到阈值的轨迹段，一眼看出反复踩踏的热点路径）、
  **边界进出点独立显示**（与轨迹主体完全分离、默认关闭，尺寸可调）与进出事件日志、
  轨迹快照对比、按坐标筛选高亮、重访格高亮（判定次数与不透明度可调）、跟随移动体视图。
- **环境演进观察**：**本步变化高亮**把「当前步相对上一步环境状态发生变化」的格子标出来
  （元胞演化、标记物生成 / 消耗、障碍物变化等），不透明度与颜色可调。
- **悬浮提示**：鼠标悬浮格子即展示该格详情，内容与当前已开启的显示项**严格同步**
  （环境状态 / 移动体 / 轨迹信息 / 标记信息逐项可开关，未开启的显示项不会出现在提示里）；
  轨迹信息除次序、首次 / 末次步数外，还给出**当前路径经过次数**（截至当前播放进度）与
  **历史累计经过次数**（整轮累计，含上次经过的步数）；
  画面中**只有一条移动体**时「历史累计经过次数」与轨迹行的整轮总数完全同值，会按
  **单蛇提示精简**开关（默认开启）自动省略，多移动体场景两条口径并存；
  可另行开启悬停行列准线（宽度可调）。

### 外观与导出

- **自定义皮肤（上传图片）**：分别上传 **蛇头** 与 **蛇身** 图片，支持 **JPG / PNG / WebP**；
  蛇头按朝向裁切贴图，蛇身以「轮廓蒙版 + 图案填充」合成，跨越边界时镜像副本同样贴图。
- **展示样式**：暗黑 / 亮色主题、轨迹 / 身体 / 箭头 / 坐标轴 / 网格线 / 起点终点 / 障碍物 / 标记物 /
  边界进出点 / 特效 / 发光 / 重访格高亮 / 悬停行列准线 / 悬浮提示等逐项开关。
- **视觉设置分类**：「视觉显示」类别下按功能拆分为 **基础视觉设置 / 高级视觉特效 / 悬停与提示 /
  色彩主题配置** 四个折叠分组，与其它类别层级完全一致（主选项卡 → 折叠分组 → 字段），
  选项按功能归类，界面不再拥挤；搜索设置项时命中项不会被藏起。
- **术语说明浮层**：统计面板的统计格与「统计项显示配置」的复选框文字带虚线下划线（`cursor: help`），
  鼠标悬停即显示该指标的统计口径说明（如「覆盖率」= 去重格子数 / 网格总格数、「CA 演进次数」=
  元胞自动机的更新代数），由 `STAT_TERMS` 术语表统一维护。
  **交互标记物状态 chips** 同样在右侧带「?」入口，悬停弹出说明面板，逐条解释该标记物的
  **功能 / 生效规则 / 使用场景**；面板复用同一浮层节点、随视口自动定位，不遮挡核心操作区域，
  其中用户自定义名称会先转义再写入，避免破坏浮层结构。
- **选项卡配色与排版**：选项卡配色抽取为 CSS 自定义属性，已激活 / 未激活两态背景色、文字色、边框色
  可逐项自定义（取色器 + 十六进制输入，附实时预览与一键重置），默认值刻意拉大两态色差；
  另提供**紧凑排版**（压缩分组 / 字段 / 开关行 / 统计格间距）与**统计切换淡入**开关。
- **统一开关行**：核心规则等板块的开关统一为「标签左 / 控件右 + 说明独占一行」的两列网格布局，
  固定行高保证所有开关、标签与说明文本落在同一基线上，对齐一致。
- **配置导入导出**：JSON 复制与下载、分享链接（`#c=…`）、粘贴 JSON 导入、前端配置诊断。
- **结果导出**：轨迹 CSV / JSON、日志 CSV / JSON、当前帧 PNG / SVG、纯轨迹 SVG、轨迹明细 CSV。

### 界面语言（国际化）

- **十种界面语言**：简体中文（源语言，默认）· 繁體中文 · English · 日本語 · 한국어 · Français ·
  Deutsch · Español · Português · Русский，顶栏右侧的语言选择器可随时切换，**无需刷新页面**；
  选择器左侧固定显示中英双语标识「Language 语言」，标识是绑定了下拉框的 `<label>`，
  点击即聚焦控件、不覆盖也不拦截其交互。
- **自动匹配系统语言**：首次打开时按 `navigator.languages` 依次匹配（如 `zh-Hant-HK` → 繁體中文、
  `en-GB` → English、`pt-BR` → Português），都匹配不到时回退英文；手动选择的结果写入 `localStorage`
  （键 `gridsneaker:locale`），下次打开优先沿用。
- **全量覆盖**：界面文案、设置标签与说明、统计与日志、弹窗、悬浮提示、导入导出说明、
  警告与错误提示全部随语言切换，运行期拼接的句子（如「结束原因：达到步数上限（300）」、
  「概率：左转 33.3% · 直行 33.3% · 右转 33.3%」）同样按目标语言重新组织语序与标点。
- **兜底提示同样多语言**：脚本完全没能启动时（兼容模式 / 内核过旧 / 禁用 JS）显示的提示块，
  由一段**不依赖 ES Module 的内联脚本**按「上次手动选择 → 浏览器语言 → 英文」直接改写，
  覆盖全部十种语言；该脚本在单文件成品里也保留。

## 快速开始

### 方式一：直接打开单文件（推荐给最终用户）

双击仓库根目录下的 `GridSneaker.html`，浏览器即可运行，无需服务器、无需联网、无需安装任何依赖。

也可以直接从 Releases 下载最新成品（附件即构建好的单文件，无需自行构建）：
`https://github.com/<用户名>/<仓库名>/releases/latest/download/GridSneaker.html`

### 方式二：本地开发（可读源码 + 热刷新）

需要 Python 3（仅用于起静态服务器）或任意静态服务器：

```bash
npm start          # 等价于 python -m http.server 8080
```

然后访问 <http://localhost:8080/>。
注意：`index.html` 通过 ES Module 加载 `src/`，**必须经 HTTP 访问**，直接双击会被浏览器的模块跨域策略拦截。

## 使用说明

### 控制条

- ▶ 播放 / 暂停 · ◀ 上一帧 · 单步 ▶ · ⟲ 重置 · ⏭ 跳到末尾 · **重新计算**（按当前配置重跑一轮）
- 循环、自动运行、跟随移动体、自适应速度
- 速度滑块（0.5 ~ 120 步/秒）与速度档位按钮（0.5 / 2 / 8 / 30 / 120）
- 随机种子（同一种子结果完全可复现）与「🎲」随机换种子
- 时间轴拖拽跳帧、当前帧 / 总帧数、结束原因与「查看结束规则」「继续运行 +20000」

> 页面加载后即按当前配置自动跑完一轮（可取消勾选「自动运行」），因此首次打开就能直接回放与观察统计。

### 键盘快捷键

| 按键 | 作用 |
| --- | --- |
| `空格` | 播放 / 暂停 |
| `←` `→` | 上一帧 / 下一帧（按住 `Shift` 一次跳 10 帧） |
| `Home` `End` | 跳到首帧 / 末帧 |
| `↑` `↓` | 播放速度提高 / 降低 |
| `-` `=` | 播放速度减半 / 加倍 |
| `R` | 回到首帧 |
| `Esc` | 暂停；焦点在设置搜索框时清空搜索内容（同「清除」按钮） |
| `Ctrl/Cmd + Z` | 撤销最近一次格子编辑（未开启格子编辑器时撤销最近的配置替换） |
| `Ctrl/Cmd + Shift + Z` · `Ctrl/Cmd + Y` | 重做上一次格子编辑 |

### 侧边面板

- **配置面板（左）**：所有设置分组**默认全部收起**，点击分组标题即可展开；
  顶部搜索框可按关键词过滤分组并自动展开命中项，清空后还原原有折叠状态；
  「清除」按钮悬停即给出完整说明（清除范围仅限搜索框内容，不改动任何已保存的设置项）；
  面板顶部另有「展开全部 / 收起全部」一键操作。
  分组顺序：场景与随机 · 网格与坐标 · 移动体与身体 · 移动规则 · 碰撞与自撞处理 ·
  多蛇生成与交互系统 · 环境感知—条件—后果规则 · 元胞自动机模式 · 结束规则（按优先级）· 展示样式
  （「展示样式」内部再分「基础视觉设置 / 高级视觉特效 / 悬停与提示 / 色彩主题配置」四个折叠分组，
  与其它类别层级一致，不再有第三层容器）。
- **统计与日志面板（右）**：预设模板 · 配置导入导出 · 导出结果 · 统计模块（含坐标筛选查询与
  「统计项显示配置」）· 轨迹快照对比 · 规则触发日志 · 配置诊断，同样默认收起。

### 边界行为

- **穿越到另一侧（默认）**：蛇头触碰画布边界时从对侧对应位置重新出现，画面会绘制方向明确的穿梭光带；
  跨缝的体节链会整体平移出镜像副本，保证蛇身视觉连续。
  **轨迹同样跨缝连续**：轨迹按「连续展开坐标 + 每段整圈平移量」解算并裁剪到网格区域，
  滑出侧一直画到边界、滑入侧从对侧接着画，因此边界网格的轨迹覆盖率是 100%，
  不会出现「靠近边界的网格只显示一半轨迹」或「跨缝轨迹整段缺失」。
- **反弹**：撞墙后掉头，永不越界；**反弹时自动启用安全避撞**——
  贴边掉头若反向格正好压在自己的身体上（撞到自己的脖子）或落在界外，
  会自动改选一个「界内 + 未被阻塞 + 不撞自身」的方向，避免被误判为自撞死亡。
  该机制仅在反弹模式下生效，穿越等其它边界策略的行为完全不变。

### 自定义皮肤

1. 展开左侧「移动体与身体 → 自定义皮肤（上传图片）」。
2. 在「蛇头皮肤」「蛇身皮肤」中点击**选择图片**，选择本地的 JPG / PNG / WebP 图片。
3. 上传后会立即渲染到画布；点**清除**可恢复纯色 / 渐变配色。

校验规则（任一不通过都会保持原皮肤不变并提示原因）：

- **格式**：仅 `.jpg` / `.jpeg` / `.png` / `.webp`；
- **体积**：单张不超过 2 MB；
- **尺寸**：每条边 16 ~ 2048 像素；
- **可解码**：文件损坏或解析失败时给出明确提示。

皮肤以 dataURL 形式保存在配置中：随「导出 JSON」一并保存，也会写入浏览器本地存储；
但**不会写入分享链接**（图片体积远超链接长度上限），分享链接载入后皮肤为未设置状态。

## 界面语言（国际化）

| 语言 | 代码 | 语言包 | 说明 |
| --- | --- | --- | --- |
| 简体中文 | `zh-CN` | `src/i18n/locales/zh-CN.js` | 源语言，界面文案即以此书写 |
| 繁體中文 | `zh-TW` | `src/i18n/locales/zh-TW.js` | 港澳台用语（匯入 / 覆蓋 / 設定 / 相容…） |
| English | `en` | `src/i18n/locales/en.js` | 非中文用户的回退语言 |
| 日本語 | `ja` | `src/i18n/locales/ja.js` | 术语按日文技术文档习惯（セル・オートマトン 等） |
| 한국어 | `ko` | `src/i18n/locales/ko.js` | 에이전트 / 뱀 머리 / 세포 자동자 / 마커 / 장애물 |
| Français | `fr` | `src/i18n/locales/fr.js` | agent / grille / marqueur / obstacle / automate cellulaire |
| Deutsch | `de` | `src/i18n/locales/de.js` | Agent / Raster / Marker / Hindernis / Kollisionsvermeidung |
| Español | `es` | `src/i18n/locales/es.js` | agente / cuadrícula / rastro / borde / evitación de colisiones |
| Português | `pt` | `src/i18n/locales/pt.js` | agente / cobra / rastro / grade / evitação de colisões |
| Русский | `ru` | `src/i18n/locales/ru.js` | агент / змейка / клеточный автомат / препятствие / след |

### 切换与持久化

- 顶栏语言选择器：左侧固定显示中英双语标识「Language 语言」（`<label for="lang-select">`，
  点击即聚焦下拉框，不覆盖控件区域、不拦截其交互），切换立即生效并同步更新 `<html lang>` 属性；
- 系统语言自动匹配：按 `navigator.languages` 顺序匹配，`zh` 家族再按脚本 / 地区细分
  （`zh-Hant`、`zh-TW`、`zh-HK`、`zh-MO` → 繁體中文，其余 → 简体中文），
  其余语言按主语言匹配区域变体（`fr-CA` → Français、`pt-BR` → Português…），都匹配不到时回退 English；
- 手动选择优先于系统语言，并持久化在 `localStorage` 的 `gridsneaker:locale`；
- 兜底提示块（`#legacy-hint`）在模块脚本没能跑起来时（兼容模式 / 内核过旧 / ES Module 被拦截）
  由内嵌经典脚本接管，走同一套「手动选择 → 浏览器语言 → 英文」优先级；
  完全禁用 JS 时脚本无法执行，此时显示 HTML 里写死的简体中文原文。

### 工作机制

界面文案直接以**中文原文写在源码里**（等价于 gettext 的 msgid），语言包以中文原文为键、译文为值，
翻译发生在**渲染边界**（`src/ui/forms.js` 的文本写入与 `title` / `placeholder` / `aria-label` 属性），
因此上千处调用点无需改动：

- 模板字面量按**位置编号**归一成占位符：`` `第 ${tick} 步` `` → 键 `第 {0} 步`，
  译文可自由调整语序（英文写成 `Step {0}`）；
- 运行期拼接的整句先精确查表，未命中则按语言包中带占位符的条目做**句式匹配**，
  捕获到的片段递归翻译，因此「日志：在前方产生 3 个障碍物」这类嵌套句子也能完整译出；
- 不含中文的字符串直接放行，用户数据（自定义命名等）不会被误翻。

### 新增一种语言

1. 以 `src/i18n/locales/en.js` 为模板新建 `src/i18n/locales/xx.js`，
   **必须保持具名导出**（如 `export const ko`），`export default` 不被单文件打包器识别；
2. 在 `src/i18n/index.js` 的 `MESSAGES` 与 `LOCALE_NAMES` 中登记（名称用该语言自身的写法）；
3. 在 `tools/i18n-extract.mjs` 的 `TARGETS` 与 `EXPORT_NAMES` 中登记同一个语言代码与导出名，
   否则 `npm run i18n:check` 不会检查该语言包；
4. 运行 `npm run i18n:extract` 重建 zh-CN 规范键集，再用 `npm run i18n:check`
   列出该语言包的**缺译 / 多译 / 空值**；
5. 若该语言从左到右以外的方向书写（阿拉伯语、希伯来语等），把代码加入 `RTL_LOCALES`，
   页面会自动设置 `dir="rtl"`——但 **RTL 版式（镜像布局）本轮未做适配**，属于后续工作；
6. 如需让降级场景也显示该语言，把兜底提示块的三条文案补进 `index.html` 内嵌的 `TEXT` 表
   （与语言包同源，改文案时两处都要改）。

语言包通过 `import` 内联进打包产物，所以**单文件版本无需联网，也不受 `file://` 下 `fetch` 被拦截的限制**。

### 译文复核建议

译文由 AI 产出，作者已核对术语一致性、占位符与标点；以下为**措辞判断空间较大**的条目，
建议由母语者复核后再定稿（括号内为当前译文）：

- **English**：`移动体`（Agent）、`元胞自动机`（Cellular automaton）、`标记物`（Marker）、
  `穿越到另一侧`（Wrap to the other side）、`反弹`（Bounce）、`覆盖率`（Coverage）、
  `得分`（Score）、`步数上限`（Step limit）、`长度策略`（Length strategy）、`陷阱`（Trap）、
  `热点轨迹过滤`（Hotspot trail filter）、`本步变化高亮`（Highlight changes this step）。
- **日本語**：`移动体`（移動体）、`穿越到另一侧`（反対側へ貫通）、`反弹`（跳ね返り）、
  `随机转向`（ランダム旋回）、`元胞自动机`（セル・オートマトン）、`标记物`（マーカー）、
  `覆盖率`（カバー率）、`长度策略`（長さ戦略）、`陷阱`（トラップ）、
  `热点轨迹过滤`（ホットスポット軌跡フィルタ）、`本步变化高亮`（今ステップの変化ハイライト）。
- **한국어**：`移动体`（에이전트）、`蛇头`（뱀 머리）、`元胞自动机`（세포 자동자）、
  `标记物`（마커）、`障碍物`（장애물）、`网格`（그리드）、`轨迹`（궤적）、
  `随机转向`（무작위 회전）、`避撞`（충돌 회피）、`自撞`（자기 충돌）。
- **Français**：`移动体`（agent）、`标记物`（marqueur）、`障碍物`（obstacle）、
  `网格`（grille）、`轨迹`（trace）、`边界`（bordure）、`避撞`（évitement des collisions）、
  `自撞`（collision avec soi-même）、`元胞自动机`（automate cellulaire）；
  引号按法文排版用 `« »`。
- **Deutsch**：`移动体`（Agent）、`网格`（Raster）、`标记物`（Marker）、`障碍物`（Hindernis）、
  `蛇`（Schlange）、`轨迹`（Spur）、`避撞`（Kollisionsvermeidung）、`自撞`（Selbstkollision）。
- **Español**：`移动体`（agente）、`蛇`（serpiente）、`网格`（cuadrícula）、`轨迹`（rastro）、
  `边界`（borde）、`避撞`（evitación de colisiones）。
- **Português**：`移动体`（agente）、`蛇`（cobra）、`轨迹`（rastro）、`网格`（grade）、
  `边界`（borda）、`避撞`（evitação de colisões）、`自撞`（autocolisão）。
- **Русский**：`移动体`（агент）、`蛇`（змейка）、`元胞自动机`（клеточный автомат）、
  `标记物`（маркер）、`障碍物`（препятствие）、`轨迹`（след）、`阈值`（порог）、
  `预设`（предустановка）。
- **繁體中文**：`导入 / 导出`（匯入 / 匯出）、`覆盖`（覆蓋）、`设定`（設定）、`兼容`（相容）
  等港澳台用语。

## 目录结构

```
.
├── index.html                 # 开发入口（ES Module，需经 HTTP 访问）
├── styles.css                 # 全站样式
├── GridSneaker.html           # 打包产物：可双击直接运行的单文件版本
├── package.json               # 版本号 / 脚本（start · test · build）
├── CHANGELOG.md               # 迭代记录
├── LICENSE                    # MIT 许可证
├── .github/
│   └── workflows/release.yml  # 打 tag 自动构建并发布成品 HTML 到 Releases
├── src/
│   ├── core/                  # 纯逻辑，无 DOM 依赖，可被 Node 直接测试
│   │   ├── grid.js            # 方格 / 六边形网格、方向、越界与环绕
│   │   ├── world.js           # 世界状态、移动体、标记物
│   │   ├── simulation.js      # 模拟主循环（逐帧纯计算）
│   │   ├── rules.js           # 移动体规则引擎
│   │   ├── conditions.js      # 条件求值
│   │   ├── ca.js              # 元胞自动机引擎
│   │   ├── actions.js         # 转向候选 / 安全避撞 / 出生点查找
│   │   ├── trail.js           # 轨迹模型与聚合
│   │   ├── score.js           # 得分与评级
│   │   ├── difficulty.js      # 拥挤度与动态难度
│   │   ├── config.js          # 默认配置 / 规范化 / 校验 / 诊断 / 分享链接
│   │   ├── config-diff.js     # 配置差异比对（模板载入改动检测）
│   │   ├── exporters.js       # CSV / JSON / SVG 导出
│   │   ├── presets.js         # 预设模板
│   │   └── rng.js             # 种子化随机数
│   ├── i18n/                  # 国际化运行时与语言包
│   │   ├── index.js           # t() / 语言注册表 / 系统语言匹配 / 持久化 / 静态骨架本地化
│   │   └── locales/           # zh-CN（规范键集）· zh-TW · en · ja · ko · fr · de · es · pt · ru
│   └── ui/
│       ├── app.js             # 应用装配：状态、控制条、面板、快捷键
│       ├── canvas.js          # Canvas 渲染器（轨迹 / 蛇身 / 皮肤 / 特效）
│       └── forms.js           # 表单与分组折叠组件、模态对话框
├── tests/core.test.mjs        # 无依赖测试（node tests/core.test.mjs）
├── tools/
│   ├── build-single-file.mjs  # 单文件打包（内联并压缩 JS / CSS）
│   ├── i18n-extract.mjs       # 抽取源码界面文案为 zh-CN 规范键集，并检查各语言包缺译
│   └── bench-trail.mjs        # 轨迹渲染性能基准（无依赖）
```

## 构建单文件

```bash
npm run build                       # 生成 GridSneaker.html（内联 + 压缩）
node tools/build-single-file.mjs --no-minify   # 生成未压缩的 GridSneaker.raw.html，便于排查问题
```

打包脚本按依赖顺序把每个 ES Module 包成 IIFE，通过 `__MODULES` 表传递导出，避免模块间同名声明冲突；
产物中的 JS / CSS 会去掉注释与缩进（保留换行以保证 ASI 语义不变），源码本身保持完整可读。
产物中的版本号取自 `package.json`，无需手工同步。
打包时只替换 `type="module"` 的入口脚本，**不依赖模块的内联经典脚本会原样保留**
（如兜底提示块的多语言改写脚本，它正是为「模块跑不起来」的场景准备的）。

## 测试

```bash
npm test              # 先校验语言包完整性（i18n 键集），再跑 node tests/core.test.mjs
npm run test:core     # 只跑源码级与行为级回归
npm run i18n:check    # 只校验语言包：规范键集是否覆盖源码、各语言是否缺译 / 多译 / 空值
```

测试为自研的无依赖框架（`ok` / `eq` / `near` / `section`），覆盖网格与边界行为、模拟主循环、
规则与条件求值、元胞自动机、得分与难度、轨迹模型、配置规范化 / 校验 / 诊断 / 差异比对、
皮肤格式校验与分享链接剥离、渲染层跨缝连续性（含边界网格轨迹覆盖率 100%）、
轨迹颜色分级 / 尖端平滑 / 边界进出点独立显示与悬浮提示同步 / 访问回溯与重访高亮等，
另含「视觉折叠分组 / 统计项显示配置 / 开关行排版 / 选项卡配色 / 统计术语浮层 / 安全避撞开关联动 /
环境写入置脏 / 播放合并重绘 / 搜索防抖 / 长跑运行提示 / 元胞状态格子放置 / 避撞处理方式 /
CA 子选项卡分类及其搜索联动 / 延迟放置（仅本步生效、不回溯历史帧）/ 第 0 步缺环境警告 /
规则集导入导出 / 环境模板 / 参数预设 / 版本号一致性（开发入口与单文件成品同步 `package.json`）」
等源码级与行为级回归断言。

国际化专项断言覆盖：语言匹配（`zh-Hant-HK` → 繁體中文、`pt-BR` → Português 等区域细分与归一）、
持久化与手动选择优先、`<html lang>` 同步、静态骨架文案本地化、占位符按位置回填与语序调整、
句式匹配与递归翻译、多行文本逐行翻译、骨架句式（纯标点拼接）、语言切换器双语标识与兜底提示块
（`data-i18n` 标注、内嵌语言表覆盖十种语言、`data-i18n-src` 查表依据、单文件产物保留该脚本）、
以及**十种语言包键集与源码文案的双向一致性**。
当前 **1869 项断言全部通过**（语言包 10 × 1670 条）。

性能基准（无依赖，可复现）：

```bash
node tools/bench-trail.mjs   # 轨迹渲染帧率与边界解算速率（同进程 A/B 对比）
```

## 部署

本项目是纯静态站点，构建产物为单个 HTML 文件，可直接部署到任意静态托管：

- **GitHub Pages**：把仓库推送到 GitHub，在 `Settings → Pages` 中选择分支（如 `main`）与根目录 `/` 保存，
  稍后访问 `https://<用户名>.github.io/<仓库名>/` 即可（`index.html` 会被自动识别为入口）。
- **GitHub Releases**：每次发版只需改 `package.json` 的 `version` 并推送同名 tag（如 `v2.12.0`），
  `.github/workflows/release.yml` 会自动校验版本号一致、跑测试、构建单文件并把 `GridSneaker.html`
  作为 Release 附件发布（因此**每个 Release 的成品都是可直接双击运行的 HTML**）。
  最新版永久下载地址：`https://github.com/<用户名>/<仓库名>/releases/latest/download/GridSneaker.html`。
- **其它静态托管**（Netlify / Vercel / Cloudflare Pages / 对象存储等）：无需构建命令，
  直接上传仓库内容或把发布目录指向仓库根目录即可。
- **离线分发**：只需分发 `GridSneaker.html` 一个文件，收件方双击即可使用。

本地预览打包产物：

```bash
python -m http.server 8080   # 访问 http://localhost:8080/GridSneaker.html
# 或直接双击 GridSneaker.html（单文件版本无同源限制）
```

## 浏览器兼容性

需要支持 ES Module、`Canvas 2D`、`ResizeObserver`、`localStorage` 的现代浏览器
（Chrome / Edge / Firefox / Safari 的近年版本）。
360、QQ 等双内核浏览器请使用**极速模式**；若脚本未能启动，页面会给出切换内核的提示。

## AI 生成说明

本项目的代码与文档目前由 AI 编程助手生成，作者负责需求定义、方案取舍、运行验证与迭代方向，并对最终提交内容负责。
项目不含第三方代码或素材。

## 更新记录

见 [CHANGELOG.md](./CHANGELOG.md)。
